---
confluence_id: "2775711896"
title: "Storyblok 开发指南"
status: current
parent_id: "2776039625"
depth: 2
domain: cms
web_url: https://castlery.atlassian.net/wiki/spaces/EC/pages/2775711896
local_joyboy_doc: null
blog_post: null
---
> updated 于

# 关于 Storyblok Visual Editor

官方文档：[https://www.storyblok.com/docs/guide/essentials/visual-editor#understanding-the-visual-editor](https://www.storyblok.com/docs/guide/essentials/visual-editor#understanding-the-visual-editor)

## Visual Editor - Preview

预览区域：实际是根据配置的网站路径嵌入了 Storyblok 编辑页面的 iframe，是与 storyblok 分离的。

### 修改配置

可以在 Settings 中的 Visual Editor 配置 iframe 的站点


![](attachment)

可以配置多个预览 URL，从而可以将** Dev, Test, UAT, Prod** 环境添加为预览源的选项。

> 原来的站点配置是基于 market，但是由于 多市场 的原因，一个 space 内可能会容纳多个 market，所以所有的 Preview URLs 都配置为不带国家域名的站点（这也是为什么如 space knight-us 会存在 US 和 CA 两个 market 的原因）

![](attachment)

### 网址结构

![](attachment)

 storyblok 会在预览链接后自动拼接当前 story 的 full_slug。
以 ① 为例：
该 story 的 slug 和 full_slug 分别为：

![](attachment)


所以在 Editor 中默认的 url 是：
[https://www-test.castlery.com/sg/general-content-v2/main-pages/our-story-brand-refresh](https://www-test.castlery.com/sg/general-content-v2/main-pages/our-story-brand-refresh) ②
但是在代码中，如果这个 story 是一个 page 级别的页面，那么会以 slug做为站点后的后缀，即：
[https://www-test.castlery.com/sg/our-story-brand-refresh](https://www-test.castlery.com/sg/general-content-v2/main-pages/our-story-brand-refresh) ③
⚠️需要注意的是：
虽然这个页面③在我们的网页中可以被正常访问到，但是由于 storyblok visual editor 的特殊性，仍会在 iframe 中访问 ②，此时 editor 中是处于 page not found。
这时候我们需要添加 real path：

![](attachment)

我们需要增加 market 前缀，以此拼接成 preview URL 的后缀，因为这是站点合法的 url。
当然，我们可以写成 sg/our-story-brand-refresh，以保持和 slug 一致，但是我们将 real path 和 slug 的概念分开，real path 是做为代码之前最后一道重写 path 的途径，这样可以让业务方自由的配置 url，或者区分出几个测试版本。

## Content Security Policy

 必须在内容安全策略指令中添加 `https://app.storyblok.com` 为 `frame-ancestor`，否则在 storyblok 中加载网站预览时会失败。

![](attachment)

## 使用 HTTPS 代理设置开发服务器

官方文档： 
背景：Storyblok v2 要求应用通过 HTTPS 提供服务

### 在 macOS 上为开发服务器设置 HTTPS 代理的分步指南

1. - 安装 mkcert 以创建有效证书，并使用 mkcert 安装 localhost
```
brew install mkcert
mkcert -install
mkcert localhost
```

1. - 安装 HTTPS 代理并使用以下命令运行代理
```
npm install -g local-ssl-proxy // Installing the proxy
local-ssl-proxy --source 7010 --target 7780 --cert localhost.pem --key localhost-key.pem
```

HTTPS 现在在端口 7010 上运行，并将请求转发到 http `7780`。

> 需要将 node 版本降级至 v14.20.1或者v18.12.0，且 check VPN 是否对连接有影响。

## Visual Editor - Content

在 Brand Refresh 后，将所有新的组件放入 folder 为 web-v2 的 folder 下，且所有 UI 组件都标记上 design 的 tag。至于 tag 为 web-v2 的组件，则为 PLA 重构期间产生，可能不会再使用。

![](attachment)

Block 分为三种类型：

- Content Type：即为 Story 级别的 container 承载元素。
- Nestable：为 container 里的子元素。
- Universal：既可做为 Content Type，也能做为 Nestable。
## Storyblok JS Bridge

官方文档：[https://www.storyblok.com/docs/guide/essentials/visual-editor#bridge-between-the-visual-editor-preview-and-content](https://www.storyblok.com/docs/guide/essentials/visual-editor#bridge-between-the-visual-editor-preview-and-content)

在项目预览和可视化编辑器内容之间建立的桥梁

主要用于两个主要任务：

1. - 允许在 HTML 元素上启用点击事件，以在可视化编辑器内容区域中打开。
2. - 监听可视化编辑器事件，以更新项目的内容或触发相关流程。
### 代码实现

#### 安装脚本

```
// libs/modules/cms/components/src/render/storyblokEditableSupport/index.tsx
const StoryblokEditableSupport = () => {
  if (isProd) {
    return null;
  }

  return <Script strategy="beforeInteractive" src="//app.storyblok.com/f/storyblok-v2-latest.js" async />;
};

export { StoryblokEditableSupport };
```

#### 初始化

##### 作用于 client side 组件的监听

- 为每个 published、change 事件重新加载 iFrame 的内容
- input：更新传入iframe 的 json 渲染内容
```
// libs/modules/cms/components/src/render/pageRender/pageRender.tsx
useEffect(() => {
  // Initialize the Storyblok JS Bridge
  const { StoryblokBridge, location } = window;
  if (StoryblokBridge) {
    const storyblokInstance = new StoryblokBridge();
    storyblokInstance.on(['published', 'change'], (event) => {
      if (!event?.slugChanged) {
        // reload page if save or publish is clicked
        location.reload();
      }
    });
    storyblokInstance.on('input', (event) => {
      // Access currently changed but not yet saved content via:
      if (event?.story?.content) {
        if (position === 'body_section') {
          setContent({ body: event?.story?.content?.body_section || [] });
        } else if (position === 'bottom_section') {
          setContent({ body: event?.story?.content?.bottom_section || [] });
        } else {
          setContent(event?.story?.content);
        }
      }
    });
  }
}, []);
```

##### 作用于 server side 组件的监听

官方文档： 

使用 nodeCache 来代替 storyblokInstance 更新 iFrame 里的内容，同时代码中可以设置在 test 环境中，接收最新的 nodeCache 代替 storyblok 接口返回的 json 内容，并且将所有的 client side 组件，套上一层 server side 的 wrapper，这样就可以实现和 client side 一致的监听逻辑。

> 目前没有采用这种方式，后续可以考虑 update。

#### Visual Editor Event Types

| Event Name | Will be emitted |
| input | after a user changes the value of a field |
| change | after the user saves the content |
| published | after the user clicks publish |
| unpublished | after the user clicks unpublish |
| enterEditmode | after the editor has been initialized in the editmode |
| customEvent | after a custom event was fired in a custom field type |


#### 在 HTML 元素上启用点击事件

在每个 HTML 元素中增加 _editable 属性，Storyblok Bridge 会自动注册跟随该 HTML元素的点击事件。

```
import { storyblokEditable } from '@storyblok/react/rsc';
<DtStack useImpression {...storyblokEditable(blok)} componentName="one-third-banner" uid={_uid} key={_uid}>
```

在初始化过程中，Storyblok Bridge 会进行解析，解析后的 JSON 将在该 HTML 元素上添加两个 HTML 属性 `data-blok-c` 和 `data-blok-uid`。

![](attachment)

# 关于 Storyblok Components/Schema

官方文档：[https://www.storyblok.com/docs/schema-configuration](https://www.storyblok.com/docs/schema-configuration)

## 所有字段类型

在 Storyblok 术语中，字段类型是最小的组成部分。它可以是一个单独的输入字段，也可以是一个自定义字段类型（插件）。

| Name | Short Description |
| Text | a basic single-line text field |
| Textarea | a multi-line text area without formatting options |
| Image | a single image upload input and access to the asset manager |
| Date/Time | a date and time picker |
| Boolean | a checkbox - true/false |
| Link | internal and external links, eMail, and anchors |
| File | a single file upload input and access to the asset manager |
| Multi-Asset | a multi asset upload input, including description for each and access to the asset manager |
| Single-Option | a dropdown of key/value pairs, Stories, Datasources, external JSONs, or Field Level Translation Languages. |
| Multi-Option | a multi select option of key/value pairs, Stories, Datasources, external JSONs, or Field Level Translation Languages. |
| Number | a number text field without formatting |
| Blocks | a field to allowed nesting of Bloks |
| Markdown | a multi-line field with formatting options resulting in Markdown format |
| Richtext | a multi-line field with formatting options that results in a JSON format |
| Group | allows you to group fields in a section (no input possibility) |
| Plugin | extend the Visual Editor Content area with your own Custom Field Type |


描述：

常用的文本字段：Text、Textarea、Richtext，一般会设置它们的 Maximum characters

### 将富文本数据呈现为 HTML

#### 实际运用

- 使用 Typography 做了一些样式的重写
- 增加了 prefix  自定义前缀
```
// libs/modules/cms/components/src/lib/component-v1/components/RichTextTypography.tsx
import React from 'react';
import { TypographyProps } from '@mui/joy/Typography';
import { Typography, Box } from '@castlery/fortress';
import { renderRichText } from '@storyblok/react';
import ReactDOMServer from 'react-dom/server';
import { SxProps } from '@mui/joy/styles/types';

type RichTextTypographyProps = TypographyProps & {
  description: any;
  sx?: SxProps;
  prefix?: any;
  [key: string]: any;
};

const RichTextTypography = ({ description, sx = [], prefix, ...rest }: RichTextTypographyProps) => (
  <Typography
    sx={[
      {
        'h1,h2,h3,h4,h5,h6,p,ol,ul,dl': {
          my: 0,
        },
        a: {
          color: 'var(--fortress-palette-brand-charcoal-500)',
          textDecoration: 'underline',
          '&:hover, &:focus': {
            color: 'var(--fortress-palette-primary-400)',
            textDecoration: 'underline',
          },
        },
      },
      ...(Array.isArray(sx) ? sx : [sx]),
      [],
    ]}
    {...rest}
  >
    <Box
      sx={{
        '> p': prefix && {
          display: 'inline',
        },
        '> p img': {
          width: '100%',
        },
        '> blockquote': {
          margin: '0 0 1rem',
        },
        '> h2': {
          b: {
            fontFamily: 'MinervaModern',
          },
        },
      }}
      dangerouslySetInnerHTML={{
        __html: (prefix ? ReactDOMServer.renderToString(prefix) : '') + renderRichText(description),
      }}
    />
  </Typography>
);

export { RichTextTypography };
```

## Presets 预设

预设定义了组件的示例数据，每个组件可以定义多个预设列表。此功能用于引导内容创建者在创建新故事时，不必始终从空白组件开始，他们可以选择其中一个预设并调整其数据。

### 创建和编辑预设

只能在内容编辑器中进行操作，可以定义预设的名称并查看现有的预设，以及哪一个被选为组件的默认预设。


![](attachment)

![](attachment)

# 与 Joyboy 的结合

## 定义 BaseService

我们先定义一个 Service（libs/modules/cms/services/src/lib/sbBaseService.ts），里面包含调用 storyblok 资源的基础方法。

> 在 baseService 里，我们对同一个返回相同的方法有 cached 和 无 cache 的方法，这里只展示无 cache 的方法。

#### constructor

```
constructor({ client }: { client: StoryblokClient }) {
  this.client = client;
  this.config = {
    isTest,
    version: isTest ? 'draft' : 'published',
  };
  this.region = `${EcEnv.NEXT_PUBLIC_COUNTRY?.toLowerCase()}/`;
}
```

#### 获取 story

```
async getStoryInRealTime<T = any>(slug: string, params?: Partial<ISbStoryParams>) {
  try {
    const { data } = await this.client.getStory(slug, {
      version: this.config.version,
      cv: getTimestamp(),
      ...params,
    });
    return data?.story as ISbStoryData<T>;
  } catch (error) {
    logger.error('Failed to getStoryInRealTime', {
      slug,
      error: error instanceof Error ? error.message : String(error),
    });
    return null;
  }
}
```

#### 获取所有在 general-content-v2 文件夹下的资源

其中整合了 storyblok 的 json 内容，并做了格式化

```
async getAllFromGeneralContentV2(): Promise<ISbMinimalStoryData[]> {
  const cacheKey = 'sb-all-stories-minimal-general-content-v2';
  return cache(
    async () => {
      try {
        const allStories = await this.getAll('cdn/stories', {
          starts_with: `${EcEnv.NEXT_PUBLIC_COUNTRY.toLocaleLowerCase()}/general-content-v2/`,
          content_type: 'page'
        });
        if (!allStories) return [];

        // 只缓存必要的数据字段，减少内存占用
        return allStories.map((story) => ({
          name: story.name,
          uuid: story.uuid,
          slug: story.slug,
          full_slug: story.full_slug,
          redirectUrl: story.content?.['redirect_url'] || null,
          path: story.path || null,
        }));
      } catch (error) {
        logger.error('Failed to fetch all stories from general content v2', {
          error: error instanceof Error ? error.message : String(error),
          context: 'storyblok_general_content_v2',
        });
        return [];
      }
    },
    [cacheKey],
    {
      tags: ['storyblok', 'all-stories', 'general-content-v2'],
      revalidate: 5 * 60,
    }
  )();
}
```

#### 获取所有资源（一般不会使用）

```
async getAll(slug: string, params?: Partial<ISbStoriesParams>) {
  try {
    const res = await this.client.getAll(slug, {
      version: this.config.version,
      ...params,
    });
    return res as ISbStoryData[];
  } catch (error) {
    logger.error('Failed to getAll', { slug, error: error instanceof Error ? error.message : String(error) });
    return null;
  }
}
```

## 定义 sbService

我们在 baseSbService 的基础上，增加了 sbService（libs/modules/cms/services/src/lib/sbService.ts），主要是为了封装不同业务逻辑对于 storyblok 资源的使用。

### Content Type 分类

我们先通过 Storyblok 的 Content type 分类，来阐述如何在代码中对于不同的 type 做出分类，以及如何封装。

#### Page

对于 Page，我们主要将其运用在 Rest Pages 以及 Home Page。

##### Rest Pages

我们先定义一个 getRestPage

```
// libs/modules/cms/services/src/lib/sbService.ts
async getRestPage(slug: string) {
  const allStories = await this.getAllFromGeneralContentV2();
  const marketPathArr = ['sg/', 'us/', 'au/', 'ca/', 'uk/'];
  const page = allStories?.find(({ slug: rawSlug, path }) => {
    if (path !== null) {
      if (marketPathArr.some((prefix) => path?.startsWith(prefix))) {
        const matchedPrefix = marketPathArr.find((prefix) => path?.startsWith(prefix));
        if (matchedPrefix) {
          const newPath = path?.slice(matchedPrefix.length);
          if (newPath === slug) {
            return true;
          }
        }
      }
    } else {
      if (rawSlug === slug) {
        return true;
      }
    }
    return false;
  });
  return page || undefined;
}
```

因为这个函数不仅要处理 brand refresh 后根据新的定义来加的 page path，也要兼容之前已存在的 page path，所以在这里对 path 做了一些额外的判断，并在这段逻辑内返回出是否有对应 path 的 page。
之后会通过 getSpecificPage，获取到对应的 page 的资源，因为 Home Page 已知了确定的路径，所以可以直接使用该函数。

```
async getSpecificPageWithoutCache(full_slug: string, params?: Partial<ISbStoryParams>) {
  return await this.getStory<PageStoryblok>(`${full_slug}`, params);
}
```

> 这里展示的是 without cache 版本，因为在 Homepage 和其他部分存在 fallback 处理，我们在后面统一讲解。

#### Blog Page

Blog page 是在 blog 下展示的页面，其实渲染方式与 page 一致，但是为了和 page 在 接口获取层面区分开来，以及 meta data 方面略有不同，所以设置了不同的 page type。
对于 Blog List，我们分成了 server side 和 client side 来处理。
在 Blog List 首页，由于我们是服务端加载，所以可以将获取结果缓存起来，以便更快进入到首页。
同样，在这个方法里，对 Blog 的数据结构也做了符合业务标准化的处理。


```
async getBlogPosts(params?: Partial<ISbStoriesParams>) {
  const { stories } = await this.getStoriesWithCache<BlogPageStoryblok>({
    starts_with: this.region + 'general-content-v2/ugc-pgc/blog/',
    content_type: sbContentTypes.BlogPage,
    page: 1,
    per_page: 9,
    ...(params || {}),
  });

  return stories.map((item) => {
    const blogBanner = item.content?.body?.find((block) => block.component === 'blog-banner') as BlogBannerStoryblok;

    return {
      key: String(item.id),
      bannerBackgroundImage: blogBanner?.image?.[0].desktop_url || '',
      name: item.name,
      url: '/' + item.slug,
      publishedAt: item.published_at === null ? '' : moment(item.published_at).format('MMM D, YYYY'),
    };
  });
}
```

而在 Blog list 的非首页，则是通过在 client side 获取，所以没有调用 baseSbService 的缓存方法。

```
async getBlogPostsOnClient(params?: Partial<ISbStoriesParams>) {
  const { stories } = await this.getStoriesWithoutCache<BlogPageStoryblok>({
    starts_with: this.region + 'general-content-v2/ugc-pgc/blog/',
    content_type: sbContentTypes.BlogPage,
    page: 1,
    per_page: 9,
    ...(params || {}),
  });
  return stories.map((item) => {
    const blogBanner = item.content?.body?.find((block) => block.component === 'blog-banner') as BlogBannerStoryblok;

    return {
      key: String(item.id),
      bannerBackgroundImage: blogBanner?.image?.[0].desktop_url || '',
      name: item.name,
      url: '/' + item.slug,
      publishedAt: item.published_at === null ? '' : moment(item.published_at).format('MMM D, YYYY'),
    };
  });
}
```

#### Visual sale page

在 Product Listing Page 中，存在类型为 Visual Sale Page 的 销售页面。他的内容与 Sale Page 基本一致，只是可以使用 storyblok 提供的 visual editor 来做 preview。

```
async getVisualSalePages(params?: Omit<ISbStoriesParams, 'content_type'>) {
  const { stories } = await this.getStoriesWithCache<VisualSalePageStoryblok>(
    {
      ...params,
      content_type: sbContentTypes.VisualSalePage,
      starts_with: `${EcEnv.NEXT_PUBLIC_COUNTRY.toLocaleLowerCase()}/general-content-v2/sale-pages/visual-sale-pages-folder/`,
    },
    {
      tags: [sbContentTypes.VisualSalePage],
    }
  );
  return stories || [];
}
```

### Storyblok 其他资源在 Joyboy 的使用

#### Header

在 Header 中有三处使用了 Storyblok 的资源：

- Notice
- Global Nav
- Menu
以 Notice 为例：

```
async getGlobalNotice(_params?: Partial<ISbStoriesParams>) {
  const slug = `${this.region}general-configuration/universal-config-new-joyboy/notice`;
  try {
    const data = await this.getStoryWithCache<MoticeGroupV2Storyblok>(slug);
    if (!data) {
      logger.error('No notice data found', { slug });
      // 尝试使用 fallback 数据
      const fallbackData = this._loadFallbackNotice();
      if (fallbackData) {
        return fallbackData;
      }
      return;
    }
    const { limit_num, notice, notice_mobile } = data.content;
    if (!notice || !limit_num || !notice_mobile) {
      logger.error('Invalid notice data structure', { slug });
      // 尝试使用 fallback 数据
      const fallbackData = this._loadFallbackNotice();
      if (fallbackData) {
        return fallbackData;
      }
      return;
    }
    return {
      noticeBarData:
        notice?.filter((item) => !isOutdated(item.published_at, item.ended_at)).slice(0, +limit_num) || [],
      noticeBarMobileData:
        notice_mobile?.filter((item) => !isOutdated(item.published_at, item.ended_at)).slice(0, +limit_num) || [],
    };
  } catch (error) {
    logger.error('Failed to fetch notice data', {
      error: error instanceof Error ? error.message : String(error),
      slug,
    });
    // API 请求失败时使用 fallback 数据
    const fallbackData = this._loadFallbackNotice();
    if (fallbackData) {
      return fallbackData;
    }
    return;
  }
}
```

在获取到 Notice 的数据之后，也是过滤出合法的数据再返回。

#### Footer

我们将 Footer 的绝大部分内容都修改至从 Storyblok 获取。避免了业务方需要修改 footer 时频繁发版。

```
async getGlobalFooter(params?: Partial<ISbStoriesParams>) {
  const slug = `${this.region}${CMS_PATHS.FOOTER}`;
  try {
    const data = await this.getStoryWithCache<FooterV2Storyblok>(slug, params);
    if (!data) {
      logger.error('No footer data found', { slug });
      // 尝试使用 fallback 数据
      const fallbackData = this._loadFallbackFooter();
      if (fallbackData) {
        return fallbackData;
      }
      return;
    }
    const { bottomList, mobileList, socialList, list } = data.content;
    return {
      footerData: list || [],
      socialList: socialList || [],
      bottomList: bottomList || [],
      mobileList: mobileList || [],
    };
  } catch (error) {
    logger.error('Failed to fetch footer data', {
      error: error instanceof Error ? error.message : String(error),
      slug,
    });
    // API 请求失败时使用 fallback 数据
    const fallbackData = this._loadFallbackFooter();
    if (fallbackData) {
      return fallbackData;
    }
    return;
  }
}
```

### 部分性能优化的组件

#### Hero

该组件用于给其内嵌组件的 image 或者 video 增加 isPreload 的标签，让他们在被 image 和 video 组件处理渲染时，能进入 Preload 的逻辑。

```
'use client';

import { StoryblokServerComponent } from '@storyblok/react/rsc';

interface HeroProps {
  blok: {
    items: any[];
  };
}

const Hero = ({ blok }: HeroProps) => {
  const { items } = blok;
  // 递归函数：遍历item里的所有值
  const processItem = (item: any): any => {
    // 如果是数组
    if (Array.isArray(item)) {
      // 检查数组长度是否大于0
      if (item.length > 0) {
        // 检查第一个元素
        const firstElement = item[0];
        // 如果第一个元素是对象
        if (typeof firstElement === 'object' && firstElement !== null) {
          // 检查是否有key为component，值为image或video
          if (firstElement.component === 'image' || firstElement.component === 'video') {
            // 给该object加上isPreload为true
            firstElement.isPreload = true;
          }
        }
      }
      // 递归处理数组中的每个元素
      return item.map((element) => processItem(element));
    }
    // 如果是对象
    else if (typeof item === 'object' && item !== null) {
      const processedItem = { ...item };
      // 遍历对象的所有属性
      for (const key in processedItem) {
        if (Object.prototype.hasOwnProperty.call(processedItem, key)) {
          processedItem[key] = processItem(processedItem[key]);
        }
      }
      return processedItem;
    }
    // 如果是基本类型，直接返回
    return item;
  };
  return (
    <>
      {items.map((item) => {
        // 处理item
        const processedItem = processItem(item);
        return <StoryblokServerComponent blok={processedItem} key={processedItem._uid} />;
      })}
    </>
  );
};

export { Hero };
```

#### Optional Block List

该组件是 DY 中 Campaign 和 Experience 的简单变体，主要用于区分顾客人群是否登录和时间范围。

```
'use client';

import { selectedActiveUser } from '@castlery/modules-user-domain';
import { isOutdated } from '@castlery/utils';
import { StoryblokServerComponent } from '@storyblok/react/rsc';
import { useSelector } from 'react-redux';

type OptionalBlockListProps = {
  blok: {
    list: {
      login_status: 'logged_in' | 'logged_out' | 'logged_in_or_out' | '';
      time_range: {
        published_at: string;
        ended_at: string;
      }[];
      widget: any[];
    }[];
  };
};

export const OptionalBlockList = ({ blok }: OptionalBlockListProps) => {
  const userLoginStatus = useSelector(selectedActiveUser);

  // 找到第一个符合条件的 item
  const firstMatchedItem = blok.list.find((item) => {
    let shouldDisplayRelativeWithTimeRange = true;
    let shouldDisplayRelativeWithLoginStatus = true;

    // 如果时间范围存在且未过期，则显示组件
    if (item.time_range.length > 0 && isOutdated(item.time_range[0].published_at, item.time_range[0].ended_at)) {
      shouldDisplayRelativeWithTimeRange = false;
    }

    // 如果登录状态为 logged_in_or_out
    if (item.login_status === 'logged_in_or_out' || item.login_status === '') {
      shouldDisplayRelativeWithLoginStatus = true;
    }

    // 如果登录状态为 logged_in
    if (item.login_status === 'logged_in') {
      if (userLoginStatus) {
        shouldDisplayRelativeWithLoginStatus = true;
      } else {
        shouldDisplayRelativeWithLoginStatus = false;
      }
    }

    // 如果登录状态为 logged_out
    if (item.login_status === 'logged_out') {
      if (!userLoginStatus) {
        shouldDisplayRelativeWithLoginStatus = true;
      } else {
        shouldDisplayRelativeWithLoginStatus = false;
      }
    }

    return shouldDisplayRelativeWithTimeRange && shouldDisplayRelativeWithLoginStatus;
  });

  // 如果找到符合条件的 item，渲染它的 widgets
  if (firstMatchedItem) {
    return firstMatchedItem.widget.map((widget) => {
      return <StoryblokServerComponent blok={widget} key={widget._uid} />;
    });
  }

  return null;
};
```

### Fallback storyblok 资源

因为现在 Joyboy 很多服务都依赖于 Storyblok 的接口返回，一旦在一秒内超过6次请求 storyblok 的同一个接口就会 429，或者 Storyblok 的接口也有可能会 500，这就会导致页面可能崩溃。
所以我们对一些比较重要的资源增加了 Fallback。以 Footer 为例，存在脚本来更新 fallback 资源，然后在接口失败，并且缓存过期的时候，会直接读取 fallback 资源来渲染，同时也会继续尝试更新接口，不会持续使用 fallback。

```
private _loadFallbackFooter(): { footerData: any[]; socialList: any[]; bottomList: any[]; mobileList: any[] } | null {
    try {
      const market = this.region.replace('/', '').toLowerCase() || 'sg';
      const fallbackData = FALLBACK_FOOTER_DATA[market];

      if (!fallbackData || !fallbackData.value) {
        logger.error('Fallback footer data not found', {
          market,
          availableMarkets: Object.keys(FALLBACK_FOOTER_DATA),
        });
        return null;
      }

      logger.warn('Using fallback footer data', {
        market,
        footerDataCount: fallbackData.value.footerData?.length || 0,
        socialListCount: fallbackData.value.socialList?.length || 0,
        bottomListCount: fallbackData.value.bottomList?.length || 0,
        mobileListCount: fallbackData.value.mobileList?.length || 0,
        lastUpdated: fallbackData.lastUpdated,
      });

      return fallbackData.value;
    } catch (error) {
      logger.error('Failed to load fallback footer data', {
        error: error instanceof Error ? error.message : String(error),
      });
      return null;
    }
  }
```