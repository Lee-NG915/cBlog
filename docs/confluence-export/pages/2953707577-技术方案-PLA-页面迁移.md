---
confluence_id: "2953707577"
title: "技术方案 - PLA 页面迁移"
status: current
parent_id: "2583822375"
depth: 1
domain: migration
web_url: https://castlery.atlassian.net/wiki/spaces/EC/pages/2953707577
local_joyboy_doc: null
blog_post: null
---
# PLA 页面迁移技术方案

## 1. 项目概述

### 1.1 背景说明

- 需将部分 PLA (Product Listing Ads) 页面从 onepiece 项目迁移至 joyboy-web
- 采用渐进式迁移策略，从新加坡区域特定页面开始
- 通过金丝雀部署确保系统稳定性
### 1.2 迁移目标

1. - 保证用户体验的平滑过渡
2. - 确保系统稳定性
3. - 支持快速回滚能力
## 2. 系统架构

### 2.1 整体架构

```
sequenceDiagram
    participant User as 用户浏览器
    participant R53 as Route 53
    participant CF as CloudFront
    participant WAF as WAF
    participant ALB as Application Load Balancer
    participant EKS as EKS Cluster

    Note over User,EKS: DNS解析阶段
    User->>R53: 1. DNS查询 www.example.com
    R53-->>User: 2. 返回CloudFront域名

    Note over User,EKS: 请求处理阶段
    User->>CF: 3. HTTPS请求
    CF->>WAF: 4. 安全检查
    WAF-->>CF: 5. 检查结果
    
    opt 静态内容
        CF-->>User: 6a. 返回缓存内容
    end

    opt 动态内容
        CF->>ALB: 6b. 转发请求
        ALB->>EKS: 7. 路由到服务
        EKS-->>ALB: 8. 返回响应
        ALB-->>CF: 9. 返回响应
        CF-->>User: 10. 返回内容
    end
```

```
flowchart TD
 subgraph ExperimentLogic["PLA Layout AB Test"]
    direction TB
        CookieCheck{"X-Exp-PlaLayout = ?"}
        CF_Func["fa:fa-code CloudFront Function<br>实验组分流"]
        SetExp["Set Cookie<br>Control:20%<br>Test A:40%<br>Test B:40%"]
        ReadCookie["Read Cookie"]
        SetHeader["Set x-exp-pla-layout"]
  end
 subgraph SharedNamespace["Prod Namespace"]
        LayoutCheck{"x-exp-pla-layout<br>= ?"}
        JoyIC["Joyboy Ingress Controller"]
        JoyCanary["Canary Ingress<br>流量切换"]
        JoyMain["joyboy Ingress"]
        JoyService["fa:fa-cubes Joyboy Service<br>新版本"]
        OnepieceService1["fa:fa-cubes Onepiece Service<br>原版本"]
        OnepieceIngress["Onepiece Ingress"]
        OneIC["Onepiece Ingress Controller"]
        OnepieceService2["fa:fa-cubes Onepiece Service<br>原版本"]
        note["通过不同的 ALB 和 Ingress Controller<br>实现流量隔离"]
  end
 subgraph EKS["EKS Cluster"]
        JoyALB["fa:fa-balance-scale Joyboy ALB"]
        OneALB["fa:fa-balance-scale Onepiece ALB"]
        SharedNamespace
  end
    Client["fa:fa-desktop Client Browser"] -- "1. DNS Query<br>www.castlery.com" --> Route53["fa:fa-route Route53<br>Hosted Zone"]
    Route53 -- "2. DNS Response<br>CloudFront Distribution" --> Client
    Client -- "3. HTTPS Request<br>www.castlery.com" --> CloudFront["fa:fa-cloud CloudFront CDN"]
    CloudFront -- "4. Route Pattern Match" --> RouteCheck{"Path Pattern Check"}
    RouteCheck -- Priority 0-2/sg/pla/* --> CF_Func
    RouteCheck -- Priority 3<br>/sg/* --> OneALB
    CF_Func -- "5 . Cookie Check" --> CookieCheck
    CookieCheck -- No --> SetExp
    CookieCheck -- Yes --> ReadCookie
    SetExp -- 6 Set Header --> SetHeader
    ReadCookie -- 6&nbsp; Set Header --> SetHeader
    SetHeader -- Forward --> JoyALB
    JoyALB -- 7a --> JoyIC
    OneALB -- 7b --> OneIC
    JoyIC -- 8 Header Check --> LayoutCheck
    LayoutCheck -- z --> JoyCanary
    LayoutCheck -- "a,b,c,..." --> JoyMain
    JoyMain --> JoyService
    JoyCanary --> OnepieceService1
    OneIC --> OnepieceIngress
    OnepieceIngress --> OnepieceService2

     CF_Func:::aws
     JoyIC:::k8s
     JoyCanary:::k8s
     JoyMain:::k8s
     JoyService:::service
     OnepieceService1:::service
     OnepieceIngress:::k8s
     OneIC:::k8s
     OnepieceService2:::service
     note:::note
     JoyALB:::aws
     OneALB:::aws
     Client:::client
     Route53:::aws
     CloudFront:::aws
     RouteCheck:::logic
    classDef aws fill:#FF9900,stroke:#232F3E,color:white
    classDef client fill:#85B09A,stroke:#232F3E,color:white
    classDef logic fill:#B5EAEA,stroke:#232F3E
    classDef experiment fill:#FFB6B9,stroke:#232F3E
    classDef eks fill:#166DE0,stroke:#232F3E,color:white
    classDef k8s fill:#326CE5,stroke:#232F3E,color:white
    classDef service fill:#4CAF50,stroke:#232F3E,color:white
    classDef note fill:#fff,stroke:#232F3E,stroke-dasharray: 5 5
    style SharedNamespace fill:#BBDEFB,stroke:#232F3E
    style ExperimentLogic fill:#FFB6B9,stroke:#232F3E
    style EKS fill:#166DE0,stroke:#232F3E,color:white
```

> ![](attachment)

现在只有 [https://www-test.castlery.com/sg/pla/remi-3-seater-sofa-with-ottoman](https://www-test.castlery.com/sg/pla/remi-3-seater-sofa-with-ottoman) 配置了,可以在这个SPU下做分流测试

## 3. 关键组件配置

### 3.1 CloudFront 配置

#### 3.1.1 Behavior Rules

```
{
  "behaviors": [
    {
      "precedence": 3,
      "pathPattern": "/sg/pla/dawson-storage-bed*",
      "origin": "joyboy-web-sg-test"
    },
    {
      "precedence": 4,
      "pathPattern": "/sg/pla/seb-extendable-dining-set-for-4-6*",
      "origin": "joyboy-web-sg-test"
    },
    // 其他 PLA 特定页面...
    {
      "precedence": 0,
      "pathPattern": "/*",
      "origin": "onepiece-sg-test"
    }
  ]
}
```

#### keyValueStore

```
{
  "pla_layout": {
    "cookie": { "name": "X-Exp-PlaLayout", "maxAge": 604800, "secure": true },
    "header": { "name": "x-exp-pla-layout" },
    "variants": {
      "z": { "weight": 20 },
      "a": { "weight": 40 },
      "b": { "weight": 40 }
    }
  },
  "theme": {
    "cookie": { "name": "X-Exp-theme", "maxAge": 604800, "path": "/" },
    "header": { "name": "x-exp-theme" },
    "variants": { "new": { "weight": 20 }, "old": { "weight": 80 } }
  }
}
```

#### 3.1.2 Viewer Request Function



![](attachment)

> cc:     我们这里使用的是 CloudFront Function , 没有使用 Lambda@Edge , 未来可能会使用Lambda来尝试调用DY

```
import cf from "cloudfront";
const kvsHandle = cf.kvs();

function getRandomValue(max) {
  return Math.floor(Math.random() * max);
}

function selectVariant(variants) {
  const totalWeight = Object.values(variants).reduce(function (sum, variant) {
    return sum + variant.weight;
  }, 0);
  const randomValue = getRandomValue(totalWeight);
  let currentWeight = 0;
  for (const variantId in variants) {
    if (variants.hasOwnProperty(variantId)) {
      const variant = variants[variantId];
      currentWeight += variant.weight;
      if (randomValue < currentWeight) {
        return variantId;
      }
    }
  }
  return Object.keys(variants)[0];
}

async function handler(event) {
  const request = event.request;
  const cookies = request.cookies || {};
  try {
    // 从KVS获取所有实验配置
    const experimentsConfig = await kvsHandle.get("EXPERIMENT_CONFIG");
    const experiments = JSON.parse(experimentsConfig);
    // 处理每个实验
    Object.entries(experiments).forEach(function (entry) {
      const experimentId = entry[0];
      const config = entry[1];
      let variant;
      // 检查现有cookie
      if (cookies[config.cookie.name]) {
        variant = cookies[config.cookie.name].value;
        // 验证变体是否有效
        if (!config.variants[variant]) {
          variant = selectVariant(config.variants);
        }
      } else {
        // 选择新的变体
        variant = selectVariant(config.variants);
      }
      // 设置实验 header
      request.headers[config.header.name] = {
        value: variant,
      };
    });
  } catch (err) {
    console.log("Error processing experiments:", err);
  }
  return request;
}
```

#### 3.1.3 Viewer Response Function

```
import cf from "cloudfront";
const kvsHandle = cf.kvs();

// 验证实验配置是否有效
function isValidExperimentConfig(config) {
    if (!config || typeof config !== "object") {
        return false;
    }

    if (
        !config.header ||
        typeof config.header !== "object" ||
        !config.header.name
    ) {
        return false;
    }

    if (!config.variants || typeof config.variants !== "object") {
        return false;
    }

    const variantKeys = Object.keys(config.variants);
    return variantKeys.length > 0;
}

// 验证变体是否有效
function isValidVariant(variant, config) {
    if (!variant || !config.variants) {
        return false;
    }
    return config.variants[variant] !== undefined;
}

// 从路径中提取配置的路径部分
function extractConfiguredPath(path, configPath) {
    if (!path || !configPath) {
        return null;
    }

    try {
        const pathParts = path.split("/");

        // 检查第一个部分是否是国家代码（两个字母）
        const hasCountryCode = pathParts[1] && /^[a-z]{2}$/.test(pathParts[1]);
        if (hasCountryCode) {
            return '/' + pathParts[1] + configPath; // 直接返回配置的路径
        }
        return null;
    } catch (error) {
        console.log("Error parsing path:", error);
        return null;
    }
}

// 构建 cookie 属性
function buildCookieAttributes(cookieConfig, requestPath) {
    if (!cookieConfig) {
        return "";
    }

    const attributes = [];

    if (cookieConfig.maxAge) {
        attributes.push(`Max-Age=${cookieConfig.maxAge}`);
    }

    if (cookieConfig.domain) {
        attributes.push(`Domain=${cookieConfig.domain}`);
    }

    // 只有在明确配置了path的情况下才处理Path属性
    if (cookieConfig.path) {
        const configuredPath = extractConfiguredPath(
            requestPath,
            cookieConfig.path
        );
        if (configuredPath) {
            attributes.push(`Path=${configuredPath}`);
        }
    }

    if (cookieConfig.secure) {
        attributes.push("Secure");
    }

    if (cookieConfig.httpOnly) {
        attributes.push("HttpOnly");
    }

    if (cookieConfig.sameSite) {
        attributes.push(`SameSite=${cookieConfig.sameSite}`);
    }

    return attributes.join("; ");
}

async function handler(event) {
    // 在 ViewerResponse 触发器中，我们只关心 response
    const response = event.response;
    if (!response) {
        console.log("Invalid event object - missing response:", event);
        return {};
    }

    // 注意：在 ViewerResponse 中，request 对象可能不完整或不存在
    const request = event.request || {};

    try {
        const experimentsConfig = await kvsHandle.get("EXPERIMENT_CONFIG");

        let experiments;
        try {
            experiments = JSON.parse(experimentsConfig || "{}");
        } catch (parseError) {
            console.log("Error parsing experiments config:", parseError);
            return response;
        }

        if (!experiments || typeof experiments !== "object") {
            console.log("Invalid experiments config format");
            return response;
        }

        Object.keys(experiments).forEach(function (experimentId) {
            try {
                const config = experiments[experimentId];

                if (!isValidExperimentConfig(config)) {
                    console.log(`Invalid experiment config for ${experimentId}`);
                    return;
                }

                // 安全地访问 headers
                const headers = request.headers || {};
                const headerValue = headers[config.header.name];
                const headerVariant = headerValue && headerValue.value;

                if (!headerVariant) {
                    return;
                }

                if (!isValidVariant(headerVariant, config)) {
                    console.log(
                        `Invalid variant ${headerVariant} for experiment ${experimentId}`
                    );
                    return;
                }

                if (!config.cookie || !config.cookie.name) {
                    return;
                }

                // 安全地访问 cookies
                const cookies = request.cookies || {};
                const existingCookie = cookies[config.cookie.name];
                const shouldSetCookie =
                    !existingCookie || existingCookie.value !== headerVariant;

                if (shouldSetCookie) {
                    if (!response.cookies) {
                        response.cookies = {};
                    }

                    const cookieAttributes = buildCookieAttributes(
                        config.cookie,
                        request.uri || ""
                    );

                    response.cookies[config.cookie.name] = {
                        value: headerVariant,
                    };

                    if (cookieAttributes) {
                        response.cookies[config.cookie.name].attributes = cookieAttributes;
                    }

                    console.log(`Cookie update for experiment ${experimentId}:`, {
                        previousValue: existingCookie ? existingCookie.value : null,
                        newValue: headerVariant,
                        reason: !existingCookie ? "initial_set" : "value_update",
                    });
                }
            } catch (experimentError) {
                console.log(
                    `Error processing experiment ${experimentId}:`,
                    experimentError
                );
            }
        });
    } catch (err) {
        console.log("Error in experiment handler:", err);
    }

    return response;
}
```

### 3.2 Kubernetes 配置

#### 3.2.1 主 Ingress 配置 (joyboy-web-ingress)

```
{{- if .Values.ingress.enabled -}}
{{- $fullName := include "joyboy-web.fullname" . -}}
{{- $onepieceServiceName := printf "onepiece-%s-onepiece-web" .Values.env -}}

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    app.kubernetes.io/name: {{ include "joyboy-web.name" . }}
    helm.sh/chart: {{ include "joyboy-web.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    nginx.ingress.kubernetes.io/canary: "false"
spec:
  ingressClassName: {{ .Values.ingress.ingressClassName }}
{{- if .Values.ingress.tls }}
  tls:
  {{- range .Values.ingress.tls }}
    - hosts:
      {{- range .hosts }}
        - {{ . | quote }}
      {{- end }}
      secretName: {{ .secretName }}
  {{- end }}
{{- end }}
  rules:
  {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
        {{- range .paths }}
          - path: {{ . }}
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ $fullName }}
                port:
                  name: http
        {{- end }}
  {{- end }}
{{- end }}
```

#### 3.2.2 金丝雀 Ingress 配置 (ingress-canary-op-pla)

```
{{- if .Values.ingress.enabled -}}
{{- $fullName := include "joyboy-web.fullname" . -}}
{{- $onepieceServiceName := printf "onepiece-%s-onepiece-web" .Values.env -}}

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}-canary-op-pla
  labels:
    app.kubernetes.io/name: {{ include "joyboy-web.name" . }}
    helm.sh/chart: {{ include "joyboy-web.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    nginx.ingress.kubernetes.io/canary: "true"
    nginx.ingress.kubernetes.io/canary-by-header: "x-exp-pla-layout"
    nginx.ingress.kubernetes.io/canary-by-header-value: "z"
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  ingressClassName: {{ .Values.ingress.ingressClassName }}
{{- if .Values.ingress.tls }}
  tls:
  {{- range .Values.ingress.tls }}
    - hosts:
      {{- range .hosts }}
        - {{ . | quote }}
      {{- end }}
      secretName: {{ .secretName }}
  {{- end }}
{{- end }}
  rules:
  {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
        {{- range .paths }}
          - path: {{ . }}
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ $onepieceServiceName }}
                port:
                  name: http
        {{- end }}
  {{- end }}
{{- end }}
```

## 4. 部署策略

### 4.1 分阶段发布计划

1. - **准备阶段**   

- 部署 Kubernetes 配置
- 配置 CloudFront Functions
- 配置监控和告警
2. - **测试阶段**   

- 部署特定 PLA 页面
- 验证系统稳定性
3. - **推广阶段** (待定)

- 逐步添加更多 PLA 页面
- 持续监控系统性能
- 收集用户反馈
### 4.2 回滚策略

1. - **快速回滚**

- 修改 CloudFront 路由规则
- 预计耗时 < 5 分钟


![](attachment)

链接缓存快速失效

```
https://www.castlery.com/sg/pla/dawson-storage-bed 

https://www.castlery.com/sg/pla/seb-extendable-dining-set-for-4-6 

https://www.castlery.com/sg/pla/dawson-pit-sectional-sofa 

https://www.castlery.com/sg/pla/dawson-bed-with-mika-side-table-set 

https://www.castlery.com/sg/pla/sloane-dining-set-for-6-8 

https://www.castlery.com/sg/pla/dawson-l-shape-sectional-sofa-with-ottoman 

https://www.castlery.com/sg/pla/joshua-chair 

https://www.castlery.com/sg/pla/mori-build-your-own-living-room-set-4-5-seater 

https://www.castlery.com/sg/pla/owen-chaise-sectional-sofa 

https://www.castlery.com/sg/pla/madison-leather-chaise-sectional-sofa 

https://www.castlery.com/sg/pla/remi-3-seater-sofa-with-ottoman
```

## 5. 监控方案

### 5.1 Datadog 监控

```
# datadog-monitor.yaml
monitors:
  - name: "PLA Pages Error Rate"
    type: metric alert
    query: >
      sum(last_5m):sum:http.server.request.errors{
        service:joyboy-web,
        path:/sg/pla/*
      } / sum:http.server.request.count{
        service:joyboy-web,
        path:/sg/pla/*
      } * 100 > 1
    message: "PLA pages error rate exceeded 1%"

  - name: "PLA Pages Response Time"
    type: metric alert
    query: >
      avg(last_5m):avg:http.server.request.duration{
        service:joyboy-web,
        path:/sg/pla/*
      } > 800
```

### 5.2 关键指标

1. - **业务指标**

- PLA 页面加载时间
- 页面错误率
- 用户转化率
2. - **系统指标**

- Pod 资源使用率
- ALB 健康状态
- Ingress 路由状态
## 6. 应急预案

### 6.1 常见问题及解决方案

| 问题类型 | 现象 | 解决方案 | 响应时间 |
| 路由异常 | 404 错误 | 检查 CloudFront 规则 | 5 分钟 |
| 性能下降 | 响应超时 | 扩容或切回旧系统 | 10 分钟 |
| 服务不可用 | 5xx 错误 | 执行快速回滚 | 5 分钟 |


### 6.2 紧急联系人

1. - 前端负责人
2. - DevOps 负责人
## 7. 后续计划

### 7.1 优化方向

1. - **监控优化**

- 完善 Datadog Dashboard
- 优化告警规则
- 添加业务指标监控
2. - **性能优化**

- CDN 缓存优化
- Ingress 配置优化
- 服务资源配置优化
### 7.2 扩展计划

1. - 完成 SG 区域全部 PLA 页面(非Bundle)迁移

1. - 具体 Bundle 的SPU需要等PIM完成
2. - 制定其他区域迁移计划
3. - 持续优化部署流程
## 8. 风险评估

### 8.1 潜在风险

1. - **技术风险**

- CDN 缓存一致性
- 路由规则冲突
- 服务性能影响
2. - **业务风险**

- SEO 影响
- 用户体验波动
- 转化率影响
### 8.2 规避措施

1. - 完善的监控体系
2. - 灰度发布策略
3. - 快速回滚机制
4. - 充分的测试覆盖