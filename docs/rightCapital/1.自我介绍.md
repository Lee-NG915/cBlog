## 自我介绍 + 工作内容

您好，我叫李东海，目前在 Castlery 担任高级前端工程师，主要负责公司核心电商平台 `Joyboy` 的前端建设。

我这几年的经历，主线一直比较聚焦在复杂前端系统的业务和工程化落地上。早期我在比心直播，主要负责活动业务、移动端 H5 和后台系统，积累了并发场景下的性能优化、公共 UI 能力建设和线上问题排查经验。到 Castlery 之后，我的工作内容更偏平台型和架构型，不只是完成单个需求，而是要去解决一个多市场电商系统在长期演进里的可维护性、稳定性和交付效率问题。

我现在在 Castlery 主要负责几个方向。

第一块是 `Joyboy` 平台重构。我们在做的是一个面向多个国家和地区的电商前端平台，既有 C 端 Web，也有 POS 场景。我参与了从 legacy 架构向 `Next.js 14 + Nx monorepo` 的渐进式重构，按业务域和分层模型去拆模块，比如 `product`、`checkout`、`payment`、`promotion` 这些核心域，重点是把边界理清楚，让系统后续能持续演进，而不是越做越重。

第二块是交易链路相关能力，我负责得比较深的模块包括 `Account`、`Promotion`、`Checkout` 和 `Payment`。像支付这一块，我们不是简单接一个支付方式，而是要处理多支付商、多市场、不同交互模型下的统一编排，所以我做了比较多关于分层设计、策略模式、Server Action 边界、异常处理和可观测性的工作。

第三块是设计系统和组件库建设。因为平台重构过程中，Web、POS、CMS 的 UI 语言需要统一，所以我也推动了组件库从 0 到 1 的建设，包括 `Design Token` 分层、`Storybook` 驱动开发、`Chromatic` 视觉回归，以及多主题能力。对我来说，这部分不仅是做组件，更重要的是把设计系统做成真正可协作、可验证、可发布的工程资产。

第四块是稳定性和工程治理。我这边也推动了前端可观测性建设，比如基于 `Sentry` 做结构化错误上报、错误分桶、告警路由，以及和 AI workflow 结合的自动化修复流程。另外也做过性能优化，包括 `ISR + Redis` 的缓存策略、搜索链路缓存、Bundle 优化和一些围绕 Core Web Vitals 的治理。

我比较擅长的组件化，以及面对复杂业务的时候，怎么把前端系统做得更稳、更清晰、更容易持续迭代。

也因为这个原因，我对 RightCapital 这种业务复杂、数据密集、对正确性和稳定性要求很高的 SaaS 产品其实挺有兴趣，我觉得我过去在复杂系统治理上的经验，能够比较自然地迁移过来。

## 代表性项目

最有代表性的项目，当前在 Castlery 参与的 `Joyboy` 电商前端平台重构。

第一，这个项目的复杂度非常高，而且是一个真正长期演进中的核心业务系统。它不是一个从 0 到 1 的理想化新项目，而是在一个已经运行多年的 legacy 基础上，去做多市场、多端形态下的渐进式重构。我们既要支撑 C 端 Web，也要兼顾 POS 场景，还要覆盖不同国家和地区的业务差异，所以这里面不只是写页面，而是要解决架构边界、模块拆分、渲染策略、缓存策略、稳定性和发布风险这些更底层的问题。

第二，这个项目比较完整地体现了我的能力结构，不只是单点开发。比如在这个项目里，我参与了按业务域和分层思想去重构 monorepo 架构，负责过 `Account`、`Promotion`、`Checkout`、`Payment` 这些核心模块；同时我也推动了设计系统和组件库建设，包括 `Design Token`、`Storybook CDD`、`Chromatic` 视觉回归、多主题能力；另外还做了可观测性和性能治理，比如 `Sentry` 结构化上报、错误分桶、`ISR + Redis` 缓存、搜索链路性能优化。这些事情放在一起，比较能说明我不是只负责某一个功能点，而是能从业务、架构和工程治理三个层面一起推进。

第三，这个项目很能体现我做技术决策时的思路。因为这种重构不能靠一次性全量重构发布，风险太大，所以我们用了渐进式迁移的方式，比如通过按模块拆分进行重构，多层 Feature Flag、模块边界约束、支付链路分层、可观测性兜底这些手段，把技术升级和业务连续性平衡起来。我觉得这类项目最能体现一个前端工程师是否具备面向复杂系统做长期建设的能力。
