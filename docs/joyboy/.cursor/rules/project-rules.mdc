---
description: Project-specific coding standards, conventions, and architectural guidelines
globs:
alwaysApply: true
---

# Project Rules - Castlery Joyboy Monorepo

这是基于 Nx Monorepo、Next.js、MUI Joy UI 的 Fortress 组件库项目规范。

## 项目架构

### Monorepo 结构

- **包管理器**: `pnpm`
- **Monorepo 工具**: Nx 20.4.5
- **工作空间**:
  - `apps/`: 应用程序（web, pos）
  - `libs/fortress/`: 基础组件库（基于 MUI Joy UI）
  - `libs/modules/`: 业务模块（按功能领域划分）
  - `libs/shared/`: 共享工具和组件
  - `packages/`: 跨项目共享包（i18n, features）

### 模块依赖规则

遵循严格的模块边界约束（通过 ESLint 规则 `@nx/enforce-module-boundaries` 强制执行）：

- `type:app` → 可以依赖所有类型
- `type:components` → 只能依赖 `shared-components`、`services`、`domain`、`util`
- `type:base-components` (Fortress) → 不依赖其他组件库
- `type:services` → 只能依赖 `shared-services`、`domain`、`util`、`config`
- `type:domain` → 只能依赖 `config`、`util`、`shared-domain`

## 代码格式化

### Prettier 配置

```json
{
  "semi": true,
  "trailingComma": "es5",
  "singleQuote": true,
  "tabWidth": 2,
  "useTabs": false,
  "printWidth": 120
}
```

### EditorConfig

- 缩进: 2 个空格
- 字符集: UTF-8
- 文件末尾换行: 是
- 移除尾随空格: 是

### 自动格式化

- **保存时自动格式化**: 已配置
- **粘贴时自动格式化**: 已配置
- **ESLint 自动修复**: 保存时自动修复

## TypeScript 配置

### 基础配置

- **目标**: `esnext`
- **模块**: `esnext`
- **Lib**: `["es2021", "dom", "DOM.Iterable"]`
- **模块解析**: `node`
- **严格模式**: 启用（可配置宽松规则）

### 路径别名

使用 `@castlery/*` 命名空间：

- `@castlery/fortress` → Fortress 组件库
- `@castlery/config` → 配置库
- `@castlery/modules-*-components` → 业务组件模块
- `@castlery/modules-*-services` → 业务服务模块
- `@castlery/modules-*-domain` → 业务领域模型

## Fortress 组件库规范

### 组件开发原则

1. **基于 MUI Joy UI**: Fortress 是基于 MUI Joy UI 的封装和扩展
2. **最小包装原则**: 如果组件不需要额外功能，直接 re-export MUI Joy 组件
3. **类型安全**: 所有组件必须正确支持 TypeScript 类型推断
4. **Ref 透传**: 所有组件必须正确支持 ref 透传（使用 `forwardRef`）

### 样式系统

#### FortressSx 响应式规范

Fortress 扩展了 MUI Joy 的 `sx` prop，支持响应式键：

```typescript
type FortressSx =
  | SxProps
  | {
      // 基础样式
      padding?: number | string;
      // 响应式样式（与 useBreakpoints hook 语义一致）
      mobile?: SxProps; // 0-599px
      tablet?: SxProps; // 600-899px
      desktop?: SxProps; // 900px+
    }
  | FortressSx[];
```

**重要**: 只有特定组件（如 `Box`、`Button`、`Grid`、`Card`、`Stack`、`List`）支持 `FortressSx`。其他组件直接使用标准的 `SxProps`。

#### 响应式断点规范

- **mobile** (xs): `0px - 599px` - `max-width: 599px`
- **tablet** (sm): `600px - 899px` - `min-width: 600px and max-width: 899px`
- **desktop** (md+): `900px+` - `min-width: 900px`

这些断点与 `useBreakpoints` hook 返回的值语义一致。

### useBreakpoints Hook

```typescript
const { mobile, tablet, desktop, xs, sm, md, lg, xl } = useBreakpoints();
```

- `mobile` = `xs` = `between('xs', 'sm')` = 0-599px
- `tablet` = `sm` = `between('sm', 'md')` = 600-899px
- `desktop` = `up('md')` = 900px+

### 组件 Props 规范

#### Polymorphic Components (component prop)

支持 `component` prop 的组件必须：

```typescript
export type ComponentProps<T extends ElementType = 'div'> = Omit<BaseProps, 'component' | 'sx'> & {
  component?: T;
  sx?: FortressSx;
} & Omit<ComponentProps<T>, keyof BaseProps | 'sx'>;

export const Component = forwardRef(function Component<T extends ElementType = 'div'>(
  props: ComponentProps<T>,
  ref: React.ForwardedRef<Element>
) {
  // ...
}) as typeof JoyComponent;
```

#### Ref 类型规范

根据实际渲染的 HTML 元素类型使用正确的 ref 类型：

- `List` → `HTMLUListElement`
- `ListItem` → `HTMLLIElement`
- `Link` → `HTMLAnchorElement`
- `Modal` → `HTMLDivElement`
- `Stepper` → `HTMLOListElement`
- `Accordion` → `HTMLDivElement`
- `Tooltip` → `HTMLDivElement`
- `Loading` → `HTMLSpanElement`

### 组件导出规范

#### 简单 Re-export

如果组件不需要任何修改，直接 re-export：

```typescript
import MComponent from '@mui/joy/Component';
export const Component: typeof MComponent = MComponent;
export type ComponentProps = ComponentProps<typeof MComponent>;
```

或者：

```typescript
export { default as Component } from '@mui/joy/Component';
export * from '@mui/joy/Component';
```

#### 包装组件

如果需要额外功能，使用 `forwardRef` 包装：

```typescript
export const Component = React.forwardRef<HTMLElement, ComponentProps>((props, ref) => {
  // 自定义逻辑
  return <MComponent ref={ref} {...props} />;
});
```

#### index.ts 导出

```typescript
export { Component } from './Component';
export type { ComponentProps } from './Component';
export * from './Component'; // 如果还有子组件或类型需要导出
```

## React 最佳实践

### 组件开发

1. **函数组件优先**: 使用函数组件 + Hooks
2. **forwardRef**: 所有需要 ref 的组件都使用 `forwardRef`
3. **displayName**: 对于复杂组件，设置 `displayName`（除非使用 `as typeof`）
4. **Memo 优化**: 谨慎使用 `React.memo`，避免过度优化

### Hooks 使用

1. **useMemo/useCallback**: 只在性能确实需要时使用
2. **useBreakpoints**: 使用项目自定义 hook 而非直接媒体查询
3. **自定义 Hooks**: 将可复用逻辑提取到自定义 hooks

### 性能优化

1. **避免不必要的 normalizedSx**: 不需要响应式支持的组件不要使用 `normalizeFortressSx`
2. **Tree-shaking**: 确保导入路径正确，支持 Tree-shaking
3. **SSR 兼容**: 响应式逻辑考虑 SSR 环境

## Storybook 规范

### Story 文件命名

- 文件: `ComponentName.stories.tsx`
- Meta: 使用 `Meta<typeof Component>` 类型
- Story: 使用 `StoryObj<typeof meta>` 类型

### Story 结构

```typescript
import type { Meta, StoryObj } from '@storybook/react';
import { Component } from './Component';

const meta: Meta<typeof Component> = {
  title: 'Fortress/Component',
  component: Component,
};

type Story = StoryObj<typeof meta>;

export default meta;

export const Default: Story = {
  args: {
    // props
  },
};
```

### Story 命名规范

- 使用中文或英文描述性名称
- 交互式 Story 使用动词开头（如 "WithInteraction"）
- 状态 Story 使用状态名（如 "Loading"、"Error"）

## Next.js 应用规范

### 项目结构

- `app/`: App Router 路由和页面
- `pages/api/`: API 路由（如果使用 Pages Router）
- `lib/`: 应用特定工具和组件
- `public/`: 静态资源

### 配置

- **Code Inspector**: 在 `next.config.js` 中配置排除 Fortress 内部文件
  ```javascript
  codeInspectorPlugin({
    exclude: [/libs\/fortress\/src/],
  });
  ```

### SSR 注意事项

- 响应式样式在 SSR 时可能不准确
- 使用 `useBreakpoints` hook 进行条件渲染时考虑 SSR 兼容性

## ESLint 规则

### 基础规则

- **模块边界**: 严格强制执行 `@nx/enforce-module-boundaries`
- **未使用变量**: 警告级别，允许 `_` 前缀忽略
- **TypeScript**: 使用 `@nx/typescript` 预设，放宽部分严格规则
  - `@typescript-eslint/no-explicit-any`: off
  - `@typescript-eslint/ban-ts-comment`: off
  - `@typescript-eslint/no-non-null-assertion`: off

### 代码质量

- `no-console`: off（允许 console）
- `react/jsx-no-useless-fragment`: off

## Git 工作流

### 提交规范

- 使用清晰的提交信息
- 功能完成后及时提交
- 相关文件一起提交（组件 + Storybook + 类型）

### 分支策略

遵循项目 Git Flow 规范（根据团队实际情况调整）

## 文档规范

### README 文件

每个库/包应该有 README.md，包含：

- 用途说明
- 安装和使用方法
- API 文档链接
- 示例代码

### 代码注释

- 使用中文注释（与用户偏好一致）
- 复杂逻辑添加注释说明
- 公共 API 添加 JSDoc 注释

## 依赖管理

### 包安装

- 使用 `pnpm` 进行包管理
- 工作空间依赖优先使用工作空间协议 `workspace:*`
- 避免在多个地方安装相同依赖的不同版本

### 版本管理

- 遵循语义化版本控制（SemVer）
- 重大更改需要更新主版本号
