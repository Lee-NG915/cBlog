# Castlery Joyboy 项目设计与规范文档

## 目录

1. [项目概述](#项目概述)
2. [架构设计](#架构设计)
3. [项目结构](#项目结构)
4. [模块依赖规则](#模块依赖规则)
5. [代码规范](#代码规范)
6. [组件库设计](#组件库设计)
7. [开发工作流](#开发工作流)
8. [工具链配置](#工具链配置)
9. [质量保证](#质量保证)

---

## 项目概述

### 项目简介

Castlery Joyboy 是一个基于 **Nx Monorepo** 架构的大型前端项目，采用 **Next.js** 作为应用框架，**MUI Joy UI** 作为基础 UI 库，并在此基础上构建了 **Fortress** 组件库系统。

### 技术栈

- **Monorepo 工具**: Nx 20.4.5
- **包管理器**: pnpm 9.15.4
- **前端框架**: Next.js 14.2.35
- **UI 基础库**: MUI Joy UI
- **状态管理**: Redux Toolkit
- **类型系统**: TypeScript 5.7.3
- **测试框架**: Jest, Playwright
- **组件文档**: Storybook 8.6.15
- **代码质量**: ESLint, Prettier

### 项目目标

1. **代码复用**: 通过 Monorepo 架构实现跨应用、跨模块的代码共享
2. **模块化设计**: 按业务领域划分模块，实现清晰的职责分离
3. **类型安全**: 通过 TypeScript 和严格的模块边界规则保证类型安全
4. **开发效率**: 通过统一的工具链和规范提升团队协作效率
5. **可维护性**: 通过清晰的架构设计和代码规范保证项目的长期可维护性

---

## 架构设计

### Monorepo 架构

项目采用 **Nx Monorepo** 架构，所有代码位于单一仓库中，通过 Nx 进行任务编排、依赖管理和构建优化。

#### 架构优势

1. **统一依赖管理**: 所有项目共享相同的依赖版本，避免版本冲突
2. **代码共享**: 业务逻辑、组件、工具函数可以在多个应用间共享
3. **原子提交**: 跨模块的更改可以在一次提交中完成，保证一致性
4. **增量构建**: Nx 的智能缓存系统只构建变更的部分，提升构建速度
5. **依赖图分析**: 自动分析项目依赖关系，识别循环依赖和架构问题

### 分层架构

项目采用清晰的分层架构，从底层到顶层依次为：

```
┌─────────────────────────────────────┐
│         Applications Layer          │  ← 应用层 (web, pos)
│  (Next.js Apps with App Router)     │
├─────────────────────────────────────┤
│        Components Layer              │  ← 组件层 (业务组件)
│  (Business Components)               │
├─────────────────────────────────────┤
│         Services Layer               │  ← 服务层 (业务逻辑)
│  (Business Services)                 │
├─────────────────────────────────────┤
│         Domain Layer                 │  ← 领域层 (领域模型)
│  (Domain Models & Redux)             │
├─────────────────────────────────────┤
│      Shared Components Layer         │  ← 共享组件层
│  (Shared Business Components)        │
├─────────────────────────────────────┤
│      Base Components Layer           │  ← 基础组件层
│  (Fortress - MUI Joy Wrapper)        │
├─────────────────────────────────────┤
│      Utilities & Config Layer        │  ← 工具和配置层
│  (Utils, Config, Types)              │
└─────────────────────────────────────┘
```

### 模块化设计

每个业务领域（如 cart、checkout、product）都按照相同的结构组织：

```
modules/{domain}/
├── components/     # UI 组件层
├── services/       # 业务服务层
└── domain/         # 领域模型层
```

这种结构确保了：
- **关注点分离**: UI、业务逻辑、数据模型各司其职
- **可测试性**: 每一层都可以独立测试
- **可复用性**: 服务层和领域层可以在不同组件中复用

---

## 项目结构

### 目录结构

```
joyboy/
├── apps/                    # 应用程序
│   ├── web/                # Web 应用 (Next.js)
│   ├── pos/                # POS 应用 (Next.js)
│   ├── web-e2e/            # Web E2E 测试
│   └── pos-e2e/            # POS E2E 测试
│
├── libs/                    # 库文件
│   ├── fortress/           # Fortress 基础组件库
│   ├── modules/            # 业务模块
│   │   ├── cart/           # 购物车模块
│   │   ├── checkout/       # 结账模块
│   │   ├── product/        # 产品模块
│   │   ├── order/          # 订单模块
│   │   ├── payment/        # 支付模块
│   │   ├── user/           # 用户模块
│   │   ├── cms/            # CMS 模块
│   │   ├── search/         # 搜索模块
│   │   ├── promotion/      # 促销模块
│   │   ├── tracking/       # 追踪模块
│   │   ├── dy/             # 动态内容模块
│   │   ├── composite/      # 复合模块
│   │   ├── retails/        # 零售模块
│   │   └── others/         # 其他模块
│   │
│   ├── shared/             # 共享库
│   │   ├── components/     # 共享组件
│   │   ├── services/       # 共享服务
│   │   ├── redux/          # Redux 相关
│   │   ├── utils/          # 工具函数
│   │   ├── types/          # 类型定义
│   │   ├── config/         # 配置
│   │   ├── observability/  # 可观测性
│   │   ├── seo/            # SEO 工具
│   │   ├── privacy-kit/    # 隐私工具
│   │   └── persistence-kit/ # 持久化工具
│   │
│   └── storybook-host/     # Storybook 宿主
│
├── packages/               # 跨项目共享包
│   ├── monorepo-i18n/      # 国际化
│   └── monorepo-features/  # 功能特性
│
├── tools/                  # 工具脚本
│   ├── plugin/             # Nx 插件
│   ├── addon-nest-controls/ # Storybook 插件
│   └── storyblok-to-storybook/ # Storyblok 转换工具
│
├── docs/                   # 文档
├── scripts/                 # 脚本文件
└── public/                  # 静态资源
```

### 应用层结构

#### Web 应用 (`apps/web`)

- **路由**: 使用 Next.js App Router (`app/` 目录)
- **中间件**: 自定义中间件处理认证、国际化等 (`middleware/`)
- **API 路由**: 服务端 API 端点 (`app/api/`)
- **配置**: Next.js 配置、Sentry 配置等

#### POS 应用 (`apps/pos`)

- 结构与 Web 应用类似，但针对 POS 场景优化
- 支持多市场配置（US, CA, AU, UK, SG）

### 库层结构

#### Fortress 组件库 (`libs/fortress`)

Fortress 是基于 MUI Joy UI 的封装和扩展，提供：

- **基础组件**: Button、Input、Card、Modal 等
- **布局组件**: Grid、Stack、Box 等
- **导航组件**: Link、Breadcrumbs 等
- **反馈组件**: Loading、Toast、Tooltip 等
- **表单组件**: Form、Select、Checkbox 等
- **扩展功能**: 响应式样式系统、主题定制等

#### 业务模块 (`libs/modules/{domain}`)

每个业务模块包含三个子模块：

1. **components**: UI 组件，依赖 Fortress 和共享组件
2. **services**: 业务逻辑服务，处理 API 调用、数据处理等
3. **domain**: 领域模型，包含 Redux store、类型定义、领域逻辑

#### 共享库 (`libs/shared`)

提供跨模块共享的功能：

- **components**: 跨业务领域的通用组件
- **services**: 通用服务（如 HTTP 客户端、缓存服务）
- **redux**: Redux 核心配置和中间件
- **utils**: 工具函数集合
- **types**: 全局类型定义
- **config**: 配置管理
- **observability**: 日志、监控、追踪
- **seo**: SEO 相关工具
- **privacy-kit**: 隐私合规工具
- **persistence-kit**: 数据持久化工具

---

## 模块依赖规则

### 依赖约束原则

项目通过 ESLint 规则 `@nx/enforce-module-boundaries` 严格强制执行模块依赖规则，确保架构的清晰性和可维护性。

### 模块类型定义

项目定义了以下模块类型（通过 `tags` 标识）：

- `type:app` - 应用程序
- `type:components` - 业务组件
- `type:shared-components` - 共享组件
- `type:base-components` - 基础组件（Fortress）
- `type:cms-components` - CMS 组件
- `type:composite-components` - 复合组件
- `type:services` - 业务服务
- `type:shared-services` - 共享服务
- `type:composite-services` - 复合服务
- `type:domain` - 领域模型
- `type:shared-domain` - 共享领域模型
- `type:composite-domain` - 复合领域模型
- `type:util` - 工具库
- `type:config` - 配置库

### 依赖规则矩阵

| 源模块类型 | 可依赖的模块类型 |
|-----------|----------------|
| `type:app` | 所有类型 |
| `type:components` | `shared-components`, `services`, `domain`, `util` |
| `type:shared-components` | `base-components`, `services`, `domain`, `util` |
| `type:base-components` | 无（不依赖其他组件库） |
| `type:cms-components` | `composite-components`, `shared-components`, `components`, `services`, `domain`, `util` |
| `type:composite-components` | `shared-components`, `components`, `services`, `domain`, `util` |
| `type:services` | `shared-services`, `domain`, `util`, `config` |
| `type:composite-services` | `services`, `shared-services`, `domain`, `util`, `config` |
| `type:domain` | `config`, `util`, `shared-domain` |
| `type:composite-domain` | `config`, `util`, `shared-domain`, `domain` |
| `type:shared-domain` | `config`, `util` |
| `type:util` | `util`, `domain` |
| `type:config` | `config`, `util` |

### 特殊规则

1. **CMS 模块**: `scope:cms` 标签的模块可以依赖所有类型（特殊权限）
2. **搜索模块**: `scope:search` 标签的模块可以依赖所有类型（特殊权限）
3. **复合模块**: `scope:composite` 可以依赖多个业务领域模块
4. **Fortress Client**: 允许 `fortress-client -> fortress` 的循环依赖（特殊处理）

### 依赖方向

依赖方向遵循以下原则：

1. **单向依赖**: 底层模块不能依赖上层模块
2. **领域驱动**: Domain 层不依赖 Components 和 Services 层
3. **组件隔离**: Base Components（Fortress）不依赖任何业务代码
4. **服务独立**: Services 层不依赖 Components 层

---

## 代码规范

### TypeScript 规范

#### 基础配置

- **目标版本**: `esnext`
- **模块系统**: `esnext`
- **Lib 支持**: `["es2021", "dom", "DOM.Iterable"]`
- **模块解析**: `bundler` (支持现代打包工具)
- **严格模式**: 启用（部分规则放宽）

#### 类型安全原则

1. **明确的类型导出**: 使用 `export type` 导出类型
2. **Ref 类型匹配**: 确保 ref 类型与实际 HTML 元素类型匹配
3. **Polymorphic Components**: 支持 `component` prop 的组件使用泛型
4. **避免 any**: 虽然规则关闭，但应尽量避免使用 `any`

#### 路径别名

使用 `@castlery/*` 命名空间统一管理导入路径：

- `@castlery/fortress` - Fortress 组件库
- `@castlery/config` - 配置库
- `@castlery/modules-{domain}-components` - 业务组件
- `@castlery/modules-{domain}-services` - 业务服务
- `@castlery/modules-{domain}-domain` - 业务领域模型
- `@castlery/shared-*` - 共享库
- `@castlery/utils` - 工具函数
- `@castlery/types` - 类型定义

### 代码格式化

#### Prettier 配置

```json
{
  "semi": true,
  "trailingComma": "es5",
  "singleQuote": true,
  "tabWidth": 2,
  "useTabs": false,
  "printWidth": 1