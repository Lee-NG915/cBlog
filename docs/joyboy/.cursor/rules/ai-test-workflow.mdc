---
description: Default AI workflow for generating regression tests with code changes
alwaysApply: true
---

# AI Test Workflow

当 AI 生成或修改代码时，默认执行以下测试流程；开发者不需要额外提醒“请写测试”。

## 默认行为

- 先判断本次 diff 是否改变运行时行为、用户可见行为、数据转换、状态流转、API 交互或错误处理。
- 只要有行为变更，必须在同一个 patch 中新增或更新相关测试；bug fix 默认写回归测试。
- 如果只是文档、注释、格式化、type-only、无运行时影响的 barrel export 调整，可以不写测试，但最终回复必须说明原因。
- 测试优先覆盖当前变更点，而不是补低价值的 `should render successfully` 或 snapshot-only 用例。

## 快速测试发现

1. 先查看改动文件同目录是否已有 `.spec.ts` / `.spec.tsx`。
2. 没有现成测试时，查看同 library 的 sibling tests，复用本项目的 mock、render helper 和断言风格。
3. 通过最近的 `project.json` 确认 Nx project name。
4. 优先把测试放在被测文件旁边。
5. 针对 bug 或分支条件，先写能保护当前回归点的 public behavior 断言。

## 测试类型选择

- React component / hook：使用 Testing Library，断言可见文本、选中状态、交互、条件渲染、callback、loading/error 分支。
- helper / mapper / util：使用 Jest table tests，覆盖正常、空值、边界和异常输入。
- Redux slice / selector：直接断言状态转移和派生值。
- listener / thunk / service / API wrapper：mock 外部边界，断言请求形态、dispatch、成功路径和失败路径。
- Next.js page / server action：优先测最近的 pure helper；当改动涉及 routing、metadata、redirect、cookies 或错误降级时补集成测试。
- 关键链路，如 checkout、payment、login、cart、跨页面流程：优先补或更新 Playwright/E2E 覆盖。

## 运行与交付

- 单个 Jest 测试文件：`pnpm nx test <project> --testFile=path/to/file.spec.tsx`。
- 同一 library 多处改动：`pnpm nx test <project>`。
- 跨 library 行为变更：先跑各自最小测试，再考虑 `pnpm quality:affected`。
- 如果测试不能运行，仍要尽量写测试，并在最终回复解释原因。
- 最终回复必须包含 `AI Test Record`：行为变更、测试新增/更新、测试命令、结果、失败/缺口、下一风险。
