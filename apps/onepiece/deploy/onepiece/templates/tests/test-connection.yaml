apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "onepiece.web.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "onepiece.web.name" . }}
    helm.sh/chart: {{ include "onepiece.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "onepiece.web.fullname" . }}:{{ .Values.web.service.port }}']
  restartPolicy: Never
