import React from 'react';
import SingleTierPromotionHint from './SingleTierBar';

const PromotionHint = (restProps) => <SingleTierPromotionHint {...restProps} />;
export default React.memo(PromotionHint);
