import React from 'react';
import SingleTierPromotionHint from './SingleTierBarV2';

const PromotionHintV2 = (restProps) => <SingleTierPromotionHint {...restProps} />;
export default React.memo(PromotionHintV2);
