import { SearchLoadingSkeleton } from '@castlery/modules-search-components';

/**
 * PLP 页面加载骨架屏
 * 使用通用的 SearchLoadingSkeleton 组件
 */
export default function PLPLoading() {
  return <SearchLoadingSkeleton />;
}
