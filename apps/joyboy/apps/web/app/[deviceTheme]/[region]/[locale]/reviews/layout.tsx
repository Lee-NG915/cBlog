import { WebMainLayout } from '@castlery/modules-cms-components/server';

export default function ReviewsLayout({ children }: { children: React.ReactNode }) {
  return <WebMainLayout>{children}</WebMainLayout>;
}
