# shared-redux-services

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-redux-services` to build the library.

## Running unit tests

Run `nx test shared-redux-services` to execute the unit tests via [Jest](https://jestjs.io).
