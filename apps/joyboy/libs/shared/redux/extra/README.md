# shared-redux-extra

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-redux-extra` to build the library.

## Running unit tests

Run `nx test shared-redux-extra` to execute the unit tests via [Jest](https://jestjs.io).
