// import { NextRequest, NextResponse } from 'next/server';
export const CastleryNameScape = 'castlery';

export const createClsSession = () => {
  // if (typeof window === 'undefined') {
  //   /* eslint-disable @typescript-eslint/no-var-requires */
  //   // const session = require('cls-hooked').createNamespace(CastleryNameScape);
  //   // session.bindEmitter(req);
  //   // session.bindEmitter(res);
  //   // session.run(() => {
  //   //   session.set('user-agent', req.headers['user-agent']);
  //   //   session.set('req', req);
  //   //   session.set('res', res);
  //   // });
  // }
  // return null;
};
export const getClsSession = () => {
  // if (typeof window === 'undefined') {
  //   /* eslint-disable @typescript-eslint/no-var-requires */
  //   const session = require('cls-hooked').getNamespace(CastleryNameScape);
  //   if (!session) {
  //     throw new Error('cls session not found');
  //   }
  //   return session;
  // }
  // return null;
};
