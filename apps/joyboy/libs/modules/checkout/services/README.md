# modules-checkout-services

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build modules-checkout-services` to build the library.

## Running unit tests

Run `nx test modules-checkout-services` to execute the unit tests via [Jest](https://jestjs.io).
