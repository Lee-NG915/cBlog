# modules-checkout-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build modules-checkout-domain` to build the library.

## Running unit tests

Run `nx test modules-checkout-domain` to execute the unit tests via [Jest](https://jestjs.io).
