import { modulesPosthogServices } from './modules-posthog-services';

describe('modulesPosthogServices', () => {
  it('should work', () => {
    expect(modulesPosthogServices()).toEqual('modules-posthog-services');
  });
});
