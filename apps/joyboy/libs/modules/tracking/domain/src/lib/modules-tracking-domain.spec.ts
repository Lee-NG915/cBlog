import { modulesTrackingDomain } from './modules-tracking-domain';

describe('modulesTrackingDomain', () => {
  it('should work', () => {
    expect(modulesTrackingDomain()).toEqual('modules-tracking-domain');
  });
});
