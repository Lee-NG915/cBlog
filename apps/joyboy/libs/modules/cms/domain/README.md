# modules-cms-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build modules-cms-domain` to build the library.

## Running unit tests

Run `nx test modules-cms-domain` to execute the unit tests via [Jest](https://jestjs.io).
