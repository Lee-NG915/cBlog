# modules-others-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build modules-others-domain` to build the library.

## Running unit tests

Run `nx test modules-others-domain` to execute the unit tests via [Jest](https://jestjs.io).
