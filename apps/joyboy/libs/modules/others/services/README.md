# modules-others-services

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build modules-others-services` to build the library.

## Running unit tests

Run `nx test modules-others-services` to execute the unit tests via [Jest](https://jestjs.io).
