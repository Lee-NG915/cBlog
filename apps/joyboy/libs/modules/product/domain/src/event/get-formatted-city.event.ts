// This file is no longer needed as we've moved the functionality to avoid dependency issues
// The original noticeCityInfoUpdated function should be imported where needed
// from the user domain directly in the components that use it

export const getFormattedCityEvent = () => {
  // This is a placeholder function to maintain compatibility
  // The actual implementation should be handled by the user domain
  console.warn('getFormattedCityEvent is deprecated and should be replaced with direct import from user domain');
};
