import { modulesDyServices } from './modules-dy-services';

describe('modulesDyServices', () => {
  it('should work', () => {
    expect(modulesDyServices()).toEqual('modules-dy-services');
  });
});
