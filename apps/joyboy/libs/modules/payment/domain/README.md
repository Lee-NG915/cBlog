# modules-payment-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build modules-payment-domain` to build the library.

## Running unit tests

Run `nx test modules-payment-domain` to execute the unit tests via [Jest](https://jestjs.io).
