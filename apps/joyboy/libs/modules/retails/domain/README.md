# modules-retails-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build modules-retails-domain` to build the library.

## Running unit tests

Run `nx test modules-retails-domain` to execute the unit tests via [Jest](https://jestjs.io).
