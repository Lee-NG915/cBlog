# modules-retails-services

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build modules-retails-services` to build the library.

## Running unit tests

Run `nx test modules-retails-services` to execute the unit tests via [Jest](https://jestjs.io).
