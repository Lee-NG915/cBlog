# modules-search-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build modules-search-domain` to build the library.

## Running unit tests

Run `nx test modules-search-domain` to execute the unit tests via [Jest](https://jestjs.io).
