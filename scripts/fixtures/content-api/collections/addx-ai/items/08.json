{
  "item": {
    "slug": "08",
    "title": "场景题与深挖卡片",
    "excerpt": "每个主讲项目按：业务问题 → 约束 → 方案 → 权衡 → 验证 → 故障/复盘。目标：单项目连续答 15 分钟。",
    "sortOrder": 8,
    "readingMinutes": 10,
    "contentVersion": 1,
    "contentMarkdown": "\n# 08 · 场景题与深挖卡片\n\n每个主讲项目按：**业务问题 → 约束 → 方案 → 权衡 → 验证 → 故障/复盘**。目标：单项目连续答 15 分钟。\n\n---\n\n## 选用哪 3 个主讲\n\n| 优先级 | 项目 | 文件 |\n| --- | --- | --- |\n| 首选 | 跨境电商架构重构 | 01 |\n| 首选 | ISR + Redis / 搜索缓存 | 04 |\n| 强差异化 | 可观测性 + Transaction + AI Harness | 03 + 09 |\n| 备选 | 设计系统 CDD | 02 |\n| 备选 | Events Book | 05 |\n| 备选 | Payment + 交易观测 | 06 |\n\n---\n\n## 卡片 A：架构重构（首选）\n\n**一句话**：legacy 按页面堆叠 → 域 + 三层 + Nx tag 可执行边界；Flag 控灰度。\n\n**可被追问的事实**\n\n- Web/POS 共用领域能力，展示层不同。  \n- 域：Product、Cart、Checkout、Payment、Promotion、Order、Search…  \n- tag + lint/CI，不是只靠文档。  \n- 灰度与新旧共存。\n\n**高概率追问 → 要点**\n\n- 为何按域不按 hooks/api？→ 改动按业务归属。  \n- 三层职责？→ 状态 / 编排 / UI。  \n- 跨域下单？→ 上层编排，domain 不互调。  \n- tag 防啥不防啥？→ 防依赖方向，不防烂抽象。  \n- 收益怎么证？→ affected 范围、违规数、交付/回归，承认学习成本。  \n- 重做调啥？→ 域粒度、composite 边界、payment 额外层。\n\n**别踩坑**：只背名词无边界冲突；声称零耦合；只讲结构不讲迁移回滚。\n\n---\n\n## 卡片 B：ISR + Redis / 搜索缓存（首选）\n\n**一句话**：多 Pod Redis 共享 Full Route Cache（1s 超时→LRU）；搜索内存 `responsesCache`（2min/100）做 TTL+in-flight 去重——**两套系统别混**。\n\n**深度笔记**：[24.ISR+Redis缓存.md](../rightCapital/24.ISR+Redis缓存.md)（解释 + 面试答 + Q 表）。速记：[04](./04-性能与缓存.md)。\n\n**可被追问的事实**\n\n- `cacheHandler`（仅 production）+ `cacheMaxMemorySize:0`；prefix=`env-buildId`。  \n- **timeoutMs=1000**（操作超时 ≠ TTL）；`estimateExpireAge→3600`；页面 revalidate 多为 **600**。  \n- connect **无超时**是已知限制。  \n- PLP/search：`force-dynamic`，不走 ISR。  \n- 搜索权威：`search-client.ts`（旧 search-result-cache 过时；勿背 5min/−30%）。  \n- INP tags：`inpPhase` / `inpBottleneck`。\n\n**高概率追问 → 要点**\n\n- 自带缓存不够？→ 单机，多 Pod 重复生成。  \n- Redis 1s 为何？→ 旁路读失败要快，不拖 TTFB。  \n- 3600 vs 600？→ 物理 TTL > stale，先 stale 再再生防击穿。  \n- Redis 挂了？→ 超时当 miss + LRU；承认跨 Pod 不一致。  \n- key？→ env/buildId；业务侧再叠 market/locale；用户态不进公共页缓存。  \n- 搜索两层？→ **同一 Map**：结果 TTL + in-flight Promise。  \n- 为何要 responsesCache？→ hydrate 二次搜闪烁 + routing 同参双 POST。  \n- 旧简历数字？→ 对齐 2min/100；讲问题动机不背 SLA。  \n\n**别踩坑**：把搜索缓存说成 Redis；Redis 当 DB；串市场；把过时文档当 SLA；说 PLP 走 ISR。\n\n---\n\n## 卡片 C：可观测性 + Transaction + AI Harness（差异化）\n\n**一句话**：结构化错误 + 11 互斥告警；交易用 step/`txn_trace_id`；AI 走 Harness 有界修复到 PR。\n\n**可被追问的事实**\n\n- `captureStructuredError` + beforeSend `error_bucket`。  \n- 交易：`trackTransaction*`；Loki 字段已有；Grafana/OTel metrics **预留**。  \n- Datadog 仍在，交易目标替换后下线。  \n- alert-harness 默认 analyze-only；biz/baseline 周 routine。  \n- 11 条互斥告警（现行规范）。\n\n**高概率追问 → 要点**\n\n- bucket 为何 beforeSend？→ 全入口一条管线。  \n- 交易 vs 站点？→ step 漏斗 + 伞状 ID；业务规则失败不上 Sentry。  \n- Grafana 跑通了吗？→ 协议与 Loki/Sentry 先行；metrics 未全落地。  \n- AI 如何防误修？→ Spec/ESLint、max 3、禁区、人审、禁 auto-merge。  \n- Harness 8 组件？→ Spec→Static→…→Evolution，Observability 是样板。  \n\n**别踩坑**：吹全自动上线；吹 Grafana SLO 已全跑；混淆 Datadog「现状」与「目标」。\n\n---\n\n## 卡片 D：设计系统（备选）\n\n**一句话**：Token 三层 + CDD + Chromatic；基础改动分级验收；缩小回归靠「少改 default + 收窄必审集」。\n\n**追问要点**：分层为何必要；API 防业务堆砌；误报处理；`'use client'`；tree shaking；Joy→Tailwind 是 ADR 非已迁完。\n\n**深挖（基础组件炸 Chromatic）**\n\n- 全量 Story ≠ 逐张人工审；契约必审、合成抽样、业务 Story 意图级联批准。  \n- 缩小真实影响面：Component Token / 新 variant / 不改 default；视觉与行为拆 PR；theme/flag 渐进。  \n- 缩小审查成本：canonical dependents + 关键路径冒烟；L0/L1 标 intentional visual 后更新基线。  \n- 意外级联（改 A 却 diff 到 B 默认态）当事故，不批量糊弄。\n\n---\n\n## 卡片 E：第三方保障 SDK（若简历有）\n\n**一句话**：统一 init/discovery/缓存/选择；降供应商耦合与重复请求。\n\n**追问要点**\n\n- 缓存放 provider 不放 Redux？→ 统一 SDK 语义与生命周期。  \n- in-flight 去重；失败不缓存。  \n- cart 变化要失效。  \n- 脚本慢/挂：超时、降级、不阻塞主购。  \n- 测：adapter mock 覆盖并发与失败。\n\n**别踩坑**：只讲 useEffect；缓存失败结果；忽略 CSP/consent/性能。\n\n---\n\n## 卡片 F：Events Book（备选）\n\n**一句话**：`addedCartActionSucceededEvent` 等成功事件 → tracking listener → 渠道 trigger。\n\n**追问要点**：单向流禁 UI 直调 SDK；Diff Report；consent 闸门；UTT conversion 现注释；pageview 用 `usePageViewOnce`。\n\n---\n\n## 卡片 G：Payment + 交易观测（备选 → 可升主讲）\n\n**一句话**：状态机 + idempotency + captureWithRetry(5s×4)；全程 `trackTransaction*`。\n\n**深度笔记**：[9.Checkout-Payment.md](../rightCapital/9.Checkout-Payment.md) 有完整代码路径、五步 pipeline、30+ 错误分类、边界情况。\n\n**追问要点**：第三方成功≠订单成功；同尝试同 key；交易告警设计走 Grafana SLI 而非再堆 Sentry domain 规则；现状 Sentry+Loki 可讲，OTel 预留；Strategy+Factory 模式；四种 ActionSchema；多市场支付差异；PCI SAQ-A 合规；库存预锁 6 分钟；Express Wallet 预创建订单。\n\n---\n\n## 卡片 H：购物车状态管理（电商核心）\n\n**一句话**：Redux slice + RTK Query 管 cart CRUD；乐观更新 + 多 Tab 同步 + 离线合并。\n\n**追问要点**\n\n- 状态放哪？→ Redux slice 持有 cart state；RTK Query 管 API 缓存和请求生命周期；cookie 存 cart token 保证跨刷新。\n- 乐观更新怎么做？→ `onQueryStarted` 里先 `updateQueryData` 改本地 → API 失败 `patchResult.undo()` 回滚。好处：用户无延迟感；风险：回滚时 UI 闪回。\n- 多 Tab 同步？→ `BroadcastChannel` / `storage` event / cookie 轮询。项目中 cart state 走 Redux，跨 Tab 需要监听 storage 或重新 fetch。\n- 离线 → 在线？→ 本地队列存操作（add/remove/update qty），上线后依次回放；冲突靠服务端 merge（last-write-wins 或版本比较）。\n- 项目联动：`addedCartActionSucceededEvent` dispatch 后 tracking listener 才触发埋点 → cart 操作失败不会发埋点。\n\n**别踩坑**：把整个 cart 存 localStorage 当持久化（token 过期、数据过时）；乐观更新不做回滚。\n\n---\n\n## 卡片 I：长列表 / 无限滚动（搜索页、PLP）\n\n**一句话**：虚拟滚动只渲染可视区域；IntersectionObserver 触发分页加载；缓存去重防重复请求。\n\n**追问要点**\n\n- 虚拟滚动原理？→ 容器高度 = `totalItems × itemHeight`；只渲染 `startIndex ~ endIndex` 的 DOM；scroll 事件更新可见范围。\n- 固定高度 vs 动态高度？→ 固定：计算简单 O(1)。动态：需要 `measureElement` + 位置缓存（`react-virtuoso` 内置支持）。\n- IntersectionObserver vs scroll 事件？→ IO 性能更好（浏览器原生、不阻塞主线程、throttle 内置）；scroll 需要手动 throttle。\n- 项目联动：搜索页用 Algolia InstantSearch 管分页 + `responsesCache`（2min/100）去重请求。不是纯虚拟滚动，是 load more + 缓存。\n- SEO？→ SSR 渲染首屏数据；客户端 load more 部分 SEO 不敏感。\n\n**别踩坑**：万级列表不做虚拟化直接全渲；scroll 事件不 throttle；不处理快速滚动导致白屏。\n\n---\n\n## 卡片 J：第三方脚本治理（GTM / DY / CookieYes）\n\n**一句话**：`next/script` 控加载时机；Consent 闸门拦未授权脚本；INP attribution 归因第三方性能影响。\n\n**追问要点**\n\n- 加载策略？→ `next/script` 三种 strategy：`afterInteractive`（hydration 后，默认）、`lazyOnload`（空闲时）、`worker`（Web Worker 隔离）。GTM 通常 `afterInteractive`；分析类可 `lazyOnload`。\n- 阻塞主线程？→ 第三方脚本是 INP/TBT 主要元凶之一。项目 INP attribution 有 `responsibility` 字段区分 `own / 3p_critical / 3p_other / extension`。\n- hydration mismatch？→ **真实案例**：CookieYes 在 hydration 前直接改 DOM → React 对比发现不一致 → 整棵树重新渲染或报错。解法：延后加载到 hydration 完成后。\n- Consent 闸门？→ `CookieYesScriptGate` + `useConsent` / `checkConsentGranted`；**无同意不加载对应脚本**（不是加载了不发事件）。分类：necessary / analytics / advertisement / functional。\n- 项目联动：DY / Algolia / Stripe / PayPal 是关键三方（`CRITICAL_THIRD_PARTY_PATTERNS`），错误**不进** `third_party` bucket，单独被告警规则覆盖。\n\n**别踩坑**：所有第三方同步加载；不区分关键/非关键三方；consent 只是 UI 没有实际拦截。\n\n---\n\n## 卡片 K：电商图片优化\n\n**一句话**：`next/image` 自动格式 + 尺寸 + lazy；CDN 变换按需裁切；hero image 设 priority 救 LCP。\n\n**追问要点**\n\n- `next/image` 做了什么？→ 自动 WebP/AVIF 格式协商；`srcset` + `sizes` 按设备宽度选图；默认 `lazy loading`（viewport 外不加载）；防 CLS（需指定 width/height 或 `fill`）。\n- LCP 优化？→ hero image / 首屏大图设 `priority={true}` → Next 自动加 `<link rel=\"preload\">`；其余图默认 lazy。\n- CDN 图片变换？→ URL 参数指定 width/quality/format → CDN 边缘实时裁切缓存 → 同一张原图生成多个变体。\n- 响应式图片？→ `sizes=\"(max-width: 768px) 100vw, 50vw\"` 让浏览器按 viewport 选合适 srcset → 移动端不下载 PC 大图。\n- 项目联动：PDP hero image 设 priority；PLP 产品卡片图 lazy；Storyblok CMS 图片走 CDN 变换。\n\n**别踩坑**：所有图都 priority（失去 lazy 优势）；不指定尺寸导致 CLS；原图直出不走 CDN 变换。\n\n---\n\n## 卡片 L：搜索自动补全设计\n\n**一句话**：debounce + abort 防竞态；最近结果 Map 缓存；键盘可导航；高亮匹配文本。\n\n**追问要点**\n\n- 竞态怎么防？→ 每次输入创建新 `AbortController`，先 `abort()` 上一次请求；结合 debounce（200-300ms）减少请求频率。\n- 缓存？→ `Map<query, results>` 或 LRU 缓存最近 N 条结果；完全匹配直接返回，不发请求。与项目 `responsesCache` 同一思想。\n- 键盘导航？→ `ArrowDown/Up` 移动高亮索引；`Enter` 选中；`Escape` 关闭。用 `aria-activedescendant` + `role=\"listbox\"` 做无障碍。\n- 高亮匹配？→ 按 query 切分结果文本 → 匹配部分用 `<mark>` 或 `<strong>` 包裹。注意：正则需转义特殊字符。\n- 空状态 / 错误？→ 空结果展示\"无匹配\"；请求失败不清空上次结果（降级体验）；loading 状态可选展示或不展示。\n- 项目联动：搜索页用 Algolia InstantSearch 的 searchBox → 内置 debounce + 请求管理；`responsesCache` 做结果缓存。\n\n**别踩坑**：不 debounce 每键一请求；不 abort 旧请求导致结果闪烁；键盘导航不做无障碍。\n\n---\n\n## 通用场景题（非 Shopify）\n\n### 没有某平台经验为何能胜任？\n\n- 承认缺口。  \n- 迁移的是：全球电商、模块化、性能、交易一致性、可观测。  \n- 语法可学，复杂治理非从零。\n\n### 你有全栈经验吗？\n\n- 高级前端 + BFF；列举 Server Actions/Redis/鉴权/支付幂等。  \n- 数据库/独立服务非主责；区分做过/参与/在学。\n\n### 如何设计全球化缓存 Key？\n\n- 资源 + market/country/language/currency + 筛选。  \n- 用户态不进公共缓存。  \n- Tag 失效 + TTL。\n\n### 从业务交付到平台架构？\n\n- 问题驱动：UI 漂移→Token/Chromatic；多 Pod→Redis；乱依赖→Nx tag；支付重试→幂等。  \n- 有代价：学习成本、一致性取舍。\n\n### 灰度发布怎么做？\n\n- Flag 三层；监控核心指标；瞬关回滚；再删旧代码。\n\n### 线上支付失败怎么排查？\n\n- Datadog 看成功率/市场/版本 → Sentry 看栈与 bucket → traceId/idempotencyKey 对单笔 → 区分第三方成功与订单状态 → 降级/Flag。\n\n### 性能优化从哪开始？\n\n- RUM CWV → 归因 LCP/INP/CLS 或接口/Bundle → 改 → 同维度对比验证。\n\n---\n\n## 比心直播经历（各 1 分钟版）\n\n按你真实经历填细节；无则改成「活动大促/电商峰值」同类叙事，勿编造。\n\n### 高并发活动前端保障\n\n- 问题：峰值流量、接口打满、首屏与脚本争主线程。  \n- 做：静态/ISR 扛展示；限流与降级开关；非关键 SDK 延后；核心路径监控与回滚 Flag。  \n- 结果：可用性优先，接受非核心功能降级。\n\n### WebSocket\n\n- 场景：实时状态（库存/直播互动/消息）。  \n- 点：重连退避、心跳、断线队列、与 HTTP 降级、鉴权与房间隔离。  \n- 坑：扇出压力、背压、乱序。\n\n### Canvas\n\n- 场景：可视化/动效/导出。  \n- 点：分层绘制、离屏、节流 rAF、内存与清晰度（DPI）、销毁监听防泄漏。  \n- 与 DOM：高频变化用 Canvas，复杂可访问性仍要 DOM 兜底。\n\n---\n\n## 简历高风险追问（速查）\n\n| 简历表述 | 准备要点 |\n| --- | --- |\n| 组件库 0→1 | 首批范围、Token、Story、Chromatic、adoption、你的决策 |\n| 核心模块负责人 | Account/Promotion/Checkout/Payment 边界；冲突与事故 STAR |\n| ISR+Redis | timeoutMs=1000、expire≈3600、revalidate≈600、connect 无超时、LRU 不一致、buildId 隔离；PLP 是 force-dynamic |\n| 搜索缓存数字 | 对齐 `search-client` 2min/100 + in-flight Promise；旧 5min/−30% 勿硬背；不进 Redis |\n| Joy→Tailwind | ADR；`@mui/joy` 仍在 |\n| DDD | 域边界，非严格 DDD |\n| AI 修复 | Harness + alert-harness；只到 PR；默认 analyze-only |\n| Grafana / 交易观测 | txn_trace_id + Sentry/Loki；OTel/SLO 规格阶段 |\n\n---\n\n## 模拟面试自检\n\n- 是否先结论后细节？  \n- 是否区分「我负责 / 团队 / 参与」？  \n- 是否有数据口径与代价？  \n- 是否讲故障与回滚？  \n- 是否虚构未做技术？\n",
    "updatedAt": "2026-08-14 07:28:49.411+00"
  }
}
