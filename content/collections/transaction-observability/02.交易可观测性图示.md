---
title: 交易可观测性图示
slug: '02'
order: 2
status: published
---

# 交易可观测性图示

* 适用范围：`checkout` + `payment`
* 目标平台：Sentry + Grafana（OTel 发射）

---

## 图例

| 标记 | 含义 |
| --- | --- |
| `[SA]` | Next.js Server Action（内部服务端） |
| `[BE]` | Payment Backend API（内部后端） |
| `[OBS]` | 可观测性平台（Sentry / Grafana / Loki / Tempo / Prometheus） |
| 无标记 | 客户端页面 / 内部 Domain Service |

## 1. 整体架构（OTel 为发射路径）

读图重点：

1. `transaction-observability helper` 是前端、Server Action、Payment Service 的统一入口。
2. OTel 负责发射 metrics / traces，日志通过 Loki 聚合。
3. Sentry 只承接错误与 trace 关联，不作为 metrics 平台。

```mermaid
flowchart LR
  subgraph UI["客户端 / Web App"]
    PP["Payment Page"]
    OWEB["OTel Web SDK"]
  end

  subgraph App["应用层 / 服务端"]
    ACT["[SA] Server Actions"]
    ONODE["OTel Node SDK"]
    PSV["PaymentService → Strategy → [BE] API"]
    LOG["pino JSON logger (trace_id + txn_trace_id)"]
  end

  subgraph Obs["可观测性层"]
    TO["transaction-observability helper"]
    COL["OTel Collector"]
    SEN["Sentry"]
    LOKI["Loki"]
    PROM["Prometheus / Mimir"]
    TEMPO["Tempo"]
    GRA["Grafana"]
  end

  PP --> TO
  ACT --> TO
  PSV --> TO
  TO --> OWEB
  TO --> ONODE
  TO --> SEN
  TO --> LOG

  OWEB -->|OTLP/HTTP| COL
  ONODE -->|OTLP| COL
  COL --> TEMPO
  COL --> PROM
  LOG --> LOKI

  LOKI --> GRA
  PROM --> GRA
  TEMPO --> GRA
  SEN -.txn_trace_id 关联.-> GRA

  classDef otel fill:#4b6cb7,color:#fff
  classDef grafana fill:#f46800,color:#fff
  classDef sentry fill:#6c3483,color:#fff
  class OWEB,ONODE,COL otel
  class GRA,LOKI,PROM,TEMPO grafana
  class SEN sentry
```

## 2. 支付主链路时序图

阅读顺序：

1. 用户选择支付方式后，先记录 `payment_method_select` 与 `payment_sdk_ready`。
2. 用户点击 Place Order 后，客户端调用 `createTransactionOrder` 创建订单。
3. 订单创建成功后，再进入 `initiatePaymentAction`。
4. 支付 Provider 返回不同 action 后，页面进入 SDK confirm、popup、success 或 redirect 分支。
5. capture 结束后，页面渲染最终支付结果，并记录 `payment_result_render`。

```mermaid
sequenceDiagram
  autonumber
  participant U as User
  participant P as Payment Page
  participant T as Transaction Helper
  participant A as [SA] Server Action
  participant S as Payment Service
  participant ST as Provider Strategy
  participant BE as [BE] Payment Backend API
  participant G as [OBS] Grafana Stack (OTel→Tempo/Prom, Loki)
  participant SE as [OBS] Sentry

  U->>P: 选择 payment method
  P->>T: payment_method_select
  P->>T: payment_sdk_ready

  U->>P: Place Order

  rect rgb(220, 255, 255)
    Note over P: createTransactionOrder — 客户端直接调用
    P->>T: create_order.started / success
  end

  P->>T: payment_initiate.started
  P->>A: initiatePaymentAction(traceId, attemptId, orderId, provider)

  rect rgb(220, 230, 255)
    Note over A,BE: OTel / Tempo 服务端追踪范围
    A->>S: processPayment(traceId)
    S->>ST: initiatePaymentIntent
    ST->>BE: /payment/initiate + x-trace-id + traceparent + sentry-trace + baggage
    BE-->>ST: paymentId + action
    ST-->>A: action schema
  end

  A-->>P: SDK_CONFIRM / SDK_POPUP / REDIRECT / SUCCESS
  P->>T: payment_initiate.success

  alt SDK_CONFIRM
    P->>T: payment_submit -> payment_sdk_confirm.started
    P->>A: capturePaymentAction(traceId, paymentId)
  else SDK_POPUP
    P->>T: payment_popup
    P->>A: capturePaymentAction(traceId, paymentId)
  else SUCCESS
    P->>A: capturePaymentAction(traceId, paymentId)
  else REDIRECT
    P->>T: payment_redirect
    P-->>U: redirect to provider
  end

  rect rgb(220, 230, 255)
    Note over A,BE: OTel / Tempo 服务端追踪范围
    A->>S: capturePayment(traceId)
    S->>ST: capturePayment
    ST->>BE: /payment/confirm + x-trace-id + traceparent + sentry-trace + baggage
    ST-->>A: success / failure / processing
  end

  T->>G: OTel span + metric + log(trace_id + txn_trace_id)

  alt capture success
    A-->>P: success
    P->>T: payment_result_render.success
  else capture failure / processing
    A-->>P: error / processing
    P->>T: payment_result_render.failure|processing
    A->>SE: captureTransactionError（system/provider error）
  end
```

## 3. Redirect Provider Callback 时序图

适用范围：

* Provider：`ZipPay`、`GrabPay`、`2C2P`
* 保留未实现：`Affirm` REDIRECT callback
* 页面形态：Next.js Server Component `page.tsx`，不是 `route.ts`

目标态：

* 使用统一的 `payment-callback` 页。
* `payment_callback_receive` 只在统一页发一次。
* 通过 `provider` 区分不同 redirect provider。
* 落点见 [transaction-instrumentation-spec.md](/transaction-observability/03) §9.2。

分支规则：

* callback 参数无效：callback 页展示错误，并记录 `payment_result_render.failure`。
* callback 成功：跳转 `/checkout/success`。
* callback 进行中：callback 页轮询支付状态，直到 success / failure。
* callback 或轮询失败：callback 页展示错误，并记录 `payment_result_render.failure`。

下图 `Callback Page` 代表收敛后的统一回调页。

```mermaid
sequenceDiagram
  autonumber
  participant P as Payment Page
  participant PR as Provider Site
  participant CB as Callback Page (Server Component)
  participant SP as Success Page
  participant T as Transaction Helper
  participant S as Payment Service
  participant BE as [BE] Payment Backend API
  participant G as [OBS] Grafana Stack
  participant SE as [OBS] Sentry

  P->>PR: redirectUrl（含 orderId / paymentId / traceId）
  PR-->>CB: returnUrl callback
  CB->>T: payment_callback_receive

  alt callback 参数无效
    CB->>T: payment_callback_receive.failure
    CB-->>CB: callback 页错误提示
    CB->>T: payment_result_render.failure
  else 有效
    rect rgb(220, 230, 255)
      Note over CB,BE: OTel / Tempo callback capture trace
      CB->>S: capturePayment(traceId, orderId, paymentId)
      S->>BE: /payment/confirm + x-trace-id + traceparent + sentry-trace
      BE-->>S: success / failure / processing
    end
    alt success
      CB-->>SP: redirect /checkout/success
    else processing
      loop 轮询直到 success / failure
        CB->>S: pollPaymentStatus(traceId, orderId, paymentId)
        S->>BE: /payment/status + x-trace-id + traceparent + sentry-trace
        BE-->>S: success / failure / processing
        S-->>CB: latest status
      end
      alt poll success
        CB-->>SP: redirect /checkout/success
      else poll failure
        CB-->>CB: callback 页错误提示
        CB->>T: payment_result_render.failure
        CB->>SE: provider_error / system_error
      end
    else failure
      CB-->>CB: callback 页错误提示
      CB->>T: payment_result_render.failure
      CB->>SE: provider_error / system_error
    end
  end

  CB->>G: log + metric + span attribute
```

## 4. 错误监控流转图

读图重点：

* 系统异常进入 Sentry，并同时写结构化日志与 OTel span。
* 业务失败和用户可恢复错误不进 Sentry，主要进入日志与 metrics。
* Grafana 通过 Loki、Prometheus / Mimir、Tempo 汇总 dashboard、alert 与 SLO。

```mermaid
flowchart TD
  A[交易事件发生] --> B{事件类型}
  B -->|系统异常| C[captureTransactionError → Sentry]
  B -->|业务失败| D[transaction event skipSentry]
  B -->|用户可恢复错误| E[result=failure]
  B -->|正常成功| F[result=success]

  C --> G[Sentry Issue / Trace]
  C --> H[结构化日志]
  D --> H
  E --> H
  F --> H

  H --> I[Loki Logs]
  D --> J[OTel metric → Prometheus]
  E --> J
  F --> J
  C --> O[OTel span → Tempo]
  E --> O

  I --> K[Grafana Dashboard / Alerting / SLO]
  J --> K
  O --> K
  G --> L[Root Cause 分析]
  K --> M[Alert] --> N[Runbook] --> L

  classDef sentry fill:#6c3483,color:#fff
  classDef grafana fill:#f46800,color:#fff
  class C,G sentry
  class I,J,K,M,O grafana
```

## 5. 说明

### 链路边界

1. `createTransactionOrder` 为客户端直接调用，非 Server Action。
2. `[SA]` Server Action 仅负责：

* `initiatePaymentAction`
* `capturePaymentAction`

3. Server Action 不包含下单逻辑。

### 关联字段

`[BE]` 请求头包含：

* `x-txn-trace-id`
* `traceparent`
* `sentry-trace`
* `baggage`

`[BE]` 响应头回传后端 `X-Request-Id`（OTel trace）。  
前端采集该字段做弱关联。

### 平台分工

* metrics / traces：由 **OTel SDK** 产生，经过 Collector 进入 Tempo / Prometheus。
* logs：进入 Loki。
* errors：进入 Sentry。
* Datadog：本方案不使用。

### 目标态说明

`payment_result_render` 在枚举中已定义，但代码尚未发射。

图中该节点为目标态。  
埋点补齐后才会产生实际数据。
